# 實作豯???實作豲???(Sunshine ESG Hub) API ?賹????v1.0

## ?蹐???

??API 版本Supabase (PostgreSQL) 實作謅?ㄞ???RESTful ?閰剁韏系統?實作謕???實作?`apikey` ??`Authorization: Bearer <JWT>` ???韏??

### 系統?系統?

- **系統?系統?**?謅Ｗ尿????謕?實作謜???Database Trigger 實作謅?????????畾????謅????
- **?????核心?**?謅Ｗ尿????RLS?謅??????系統實作謕?功能撖∵????????謕?系統?
- \*_實作?_?謅Ｗ??實作豯凋?????實作?察??????橫???蝓蹎???謕?系統?系統?

---

## ?甇察狀??實作?

### JWT Token

```javascript
const { data, error } = await supabase.auth.signInWithPassword({
  email: 'user@company.com',
  password: 'password',
});
```

### ?嚚?????韏?

```javascript
headers: {
  'Authorization': `Bearer ${session.access_token}`,
  'apikey': process.env.SUPABASE_ANON_KEY
}
```

---

## 實作??? (Resources)

### 1. ESG 實作????(Readings)

#### GET /rest/v1/esg_readings

系統? ESG ?謜蹎折?實作?謕???Supabase ???????謕??畸狗鞊舀????

\*_??謕????_

- `org_unit_id`: `eq.{uuid}` (??謕頩????????????
- `status`: `eq.approved` (??謕頩????????????
- `select`: `*, metric:metric_definitions(name, unit), org:org_units(name)` (?謢????璇???郁?????謕頩功能

\*_???????_

```javascript
// 系統?實作?謕?系統?實作?謕?系統?系統?
const { data, error } = await supabase
  .from('esg_readings')
  .select(
    `
    id, value, calculated_value, status, period_start,
    metric:metric_definitions(name, unit),
    org:org_units(name)
  `
  )
  .eq('status', 'approved');
```

#### POST /rest/v1/esg_readings

?謜蹎折?實作??雓??ESG 實作?

**?嚚???????**

```json
{
  "metric_id": "uuid",
  "org_unit_id": "uuid",
  "period_start": "2025-01-01",
  "period_end": "2025-01-31",
  "value": 1250.5,
  "target_value": 1200.0
}
```

\*_?謚?????_ ?嚚??系統?`calculated_value`?謅????????實作畾?????

#### PATCH /rest/v1/esg_readings

??謕雓實作謅???實作????實作?謕???實作?

---

### 2. ?????系統? (Organization Units)

#### GET /rest/v1/org_units

實作????實作?????

#### POST /rest/v1/org_units

實作????實作謅?????Admin?謅????

---

### 3. ESG 實作????(Metric Definitions)

#### GET /rest/v1/metric_definitions

實作???謕?實作????

---

## ????功能?謕??(RPC)

### process_approval

系統?系統?實作???頩??實作??????

\*_???????_ /rest/v1/rpc/process_approval

\*_實作?_ POST

\*_??謕????_

```json
{
  "reading_id": "target-uuid",
  "action": "approve" // 系統?: "submit", "approve", "reject"
}
```

\*_???????_

- `approve/reject`: 系統? Admin
- `submit`: 系統?系統?實作?(Editor)

### get_esg_stats

系統?功能?????實作?

\*_???????_ /rest/v1/rpc/get_esg_stats

\*_??謕頩???_

```json
{
  "totalEmission": 9.08,
  "pendingReviews": 1,
  "dataCompleteness": 98
}
```

---

## 實作?穿???

### ESG Readings

```typescript
interface ESGReading {
  id: string;
  metric_id: string;
  org_unit_id: string;
  period_start: string;
  period_end: string;
  period_type: 'monthly' | 'quarterly' | 'yearly';
  value: number;
  target_value?: number;
  calculated_value?: number; // ??Trigger 實作畾???
  factor_used?: number; // ?頛舀??實作?
  status: 'draft' | 'review' | 'approved' | 'rejected' | 'locked';
  created_by?: string;
  approved_by?: string;
  created_at: string;
  updated_at: string;
}
```

### Metric Definitions

```typescript
interface MetricDefinition {
  id: string;
  code: string; // 實作實作??? 'E-GHG-scope1'
  name: string; // ?頛????謕?擗?
  category: 'Environmental' | 'Social' | 'Governance';
  unit: string; // 實作謅??? 'tCO2e', 'kWh', '%'
  description?: string;
  standard_ref?: string; // GRI ?????系統?
  is_active: boolean;
}
```

---

## 功能?系統系統?

| HTTP Code | 功能      | ??謕?系統?           |
| --------- | ---------- | ------------------ |
| 200       | 系統?       | ?嚚??系統?系統?       |
| 401       | 實作?     | Token 實作?謕雓核心|
| 403       | ??暹擗?謘???   | 系統? RLS ??氯??     |
| 409       | ?????       | 系統?系統?實作?     |
| 422       | 系統?系統?   | ????功能?謕??      |
| 500       | ??ㄜ????謕???? | ?賹????謕?功能      |

---

## 系統實作????

### 實作?Supabase

```typescript
import { createClient } from '@supabase/supabase-js';

const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
);
```

### ?謜蹎折?實作?????

```typescript
const submitReading = async (data: {
  metricId: string;
  orgId: string;
  periodStart: string;
  periodEnd: string;
  value: number;
}) => {
  const { error } = await supabase.from('esg_readings').insert({
    metric_id: data.metricId,
    org_unit_id: data.orgId,
    period_start: data.periodStart,
    period_end: data.periodEnd,
    value: data.value,
  });

  if (!error) {
    console.log('實作??????????????系統?實作畾???');
  }
};
```

### ?豯∴?????謕?????謘?韏?

```typescript
const approveReading = async (readingId: string) => {
  const { error } = await supabase.rpc('process_approval', {
    reading_id: readingId,
    action: 'approve',
  });
};
```

### 實作?謕?系統?

```typescript
const fetchReport = async () => {
  const { data } = await supabase
    .from('esg_readings')
    .select(
      `
      period_start,
      value,
      calculated_value,
      metric:metric_definitions(name, unit),
      org:org_units(name)
    `
    )
    .eq('status', 'approved')
    .order('period_start', { ascending: false });
};
```

---

## 系統實作?頛荒???

### 系統實作????

```bash
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

### Supabase ?蹎????獢???

1. 系統?RLS (Row Level Security)
2. ?獢??? Storage Bucket: `esg-evidence` (?謢???)
3. ?獢??? Authentication Providers
4. 系統? `supabase-schema.sql` 系統?系統?核心

---

## ??謕?系統?

### ??謕?系統?

- ?頛舀???`select` 系統?功能??????
- 系統?Supabase 實作?謕鞎撚謘??秋??蹎抆?實作
- ?蹎?雓??撖∵?實作謅?ㄜ?實作?(`range()`)

### ?撖?????氯??

- 系統?實作謅???實作????????實作????
- ?????實作?謕???SWR ??React Query
- ??謕?鞈郁??? Supabase Realtime ?畾?雓系統?版本

---

## ??謕雓核心

| 版本| 系統?       | ???雓?                  |
| ---- | ---------- | ---------------------- |
| v1.0 | 2025-01-04 | 實作?謕雓???擳???ESG 功能|

---

\*??API ?蝘?韏??JunAiKey ?賹??系統?實作謅?ㄡ頩???瘞?蝬??豰刈系統?系統?實作蹎抆????謕?????系統?
