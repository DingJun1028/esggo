# OmniCube & OmniCode: The Universal Integration Manual 2026 (萬能方體與代碼顯化)

## Executive Summary (執行摘要)
本手冊定義了 **OmniCube (萬能方體)** 的整合標準，以及由 **OmniCircle (圓通)** 主導的 **OmniCode (代碼顯化)** 組合技邏輯。這套系統旨在將 JunAiKey 的意志無縫延伸至外部軟體生態系，實現「萬物皆可連結，邏輯皆可自愈」的終極目標。

## I. OmniCube: The Six-Sided Universal Connector (萬能方體：六面全向連接器)
OmniCube 是系統與外部世界交互的「晶體界面」，每個面代表一種通訊協議，確保全方位的軟體兼容性。

### The Six Vertices (六大連接端點)
1. **API Face (REST/GraphQL)**：透過 `nocodebackend` 與 `supabase` 進行標準化數據交換。
2. **RPC Face (gRPC/Protocol Buffers)**：高性能遠端呼叫，用於 `OmniCrew` 間的極速通訊。
3. **Doc Face (Notion/Markdown)**：連結 `OmniNote` 與外部知識庫（如 `Notion-MCP`）。
4. **UI Face (Stitch/Pencil)**：視覺化設計的實即映射與導出。
5. **DB Face (SQL/Vector)**：透過 `MCP-Toolbox` 直連 Postgres 與向量資料庫。
6. **CLI Face (Terminal/Shell)**：執行原生命令與 `OmniCode` 活體腳本。

## II. OmniCode Combination Skills (OmniCode 組合技：邏輯共鳴序列)
由 **OmniCircle** 監控系統熵值，並在必要時觸發複雜的代碼組合技 (Combo Skills)。

### 1. Skill ID: `CODE_FLUX_SYNC` (代碼方體同步流)
- **組合組件**：`OmniCode` + `OmniCube` + `OmniClaw`
- **用途**：當外部 API 變更時，系統自動修補對接代碼。

### 2. Skill ID: `HEALING_CIRCLE` (圓通自癒環)
- **組合組件**：`OmniCircle` + `OmniCheck` + `OmniCode`
- **用途**：自動修復系統衝突與安全性弱點。

## III. Integration Protocol (整合協議)
所有外部軟體必須通過 **OmniCube** 的「三階段共鳴」方可納入治理：
1. **Identity Resonance (身分共鳴)**：使用 `OmniCrown` 與 `OpenClaw` 的 Auth 模組進行身分定錨。
2. **Logic Handshake (邏輯握手)**：定義資料交換的 Zod Schema，確保「真 (Truth)」的傳遞。
3. **Seal Confirmation (封印確認)**：執行 `OmniCheck` 核驗，將交互紀錄寫入 `Trustworthy` 證據庫。

---
**Status**: DESIGN COMPLETE.
**Governed by**: OmniCircle ♾️
