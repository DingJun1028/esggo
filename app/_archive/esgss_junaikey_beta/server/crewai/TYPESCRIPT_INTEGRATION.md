# TypeScript ? CrewAI 實作?謕????遴???

**實作\*: ESGss x JunAiKey Beta ?賹???
**系統?\*\*: TypeScript (?豲??蹇?? + Python (CrewAI)

---

## ?????謜???實作????

### 實作擗????謕???謕鞊梯????荒?

```
TypeScript ?賹???系統? Python CrewAI
     (?豲??)          (AI ???)
```

**系統?**:

- ??CrewAI ?嚚?鞊??Python 系統?
- ??TypeScript 實作?渡????????
- ??系統? HTTP/Webhook 系統系統?

---

## ?? ??謕憸?豰?謢察?

### Python 實作謅?rewAI ?????

```
server/crewai/
實作??esg_crew.py              # Python ???系統?
實作??tasks/
??  實作??esg_tasks.py
??  實作??thousand_face_tasks.py
實作??requirements.txt
實作??README.md
```

### TypeScript 實作謅????謕?謚??

```
server/
實作??types/
??  實作??crewai.types.ts      # ??CrewAI ??渡????????
實作??services/
??  實作??CrewAIClient.ts      # ??TypeScript ????謅??
實作??webhooks/
??  實作??crewai_receiver.ts   # ??Webhook ??謕????
實作??server.ts
```

---

## ?? 實作謍???

### 1. TypeScript ??寞???Python CrewAI

```typescript
// server/services/ESGReportService.ts
import { getCrewAIClient } from './CrewAIClient';

async function generateESGReport(organizationId: string) {
  const client = getCrewAIClient();

  // ??寞???Python CrewAI
  const result = await client.generateESGReport({
    organizationId,
    frameworks: ['GRI', 'SASB', 'TCFD'],
  });

  // ??蛔????????
  const finalResult = await client.waitForCompletion(result.executionId);

  return finalResult.result;
}
```

### 2. Python CrewAI 實作豲?????

```
1. TypeScript ??謕?頩??????Python CrewAI
2. Python ??謕?實作??
3. Python 系統? Webhook ??謕頩正豰?謢察? ??TypeScript
4. TypeScript ??謕????遴?????察???3+1 實作?
```

---

## 實作?謕????遴???

### 1. ??渡??????????謑e Safety??

**???系統系統?**:

```typescript
import { ESGReportRequest, ESGReportResult } from '../types/crewai.types';

async function createReport(req: ESGReportRequest): Promise<ESGReportResult> {
  // ??ㄞ??潘鞊舀??系統?
}
```

**????謕?系統?**:

```typescript
async function createReport(req: any): Promise<any> {
  // ??????鞊舀???????
}
```

---

### 2. ??謕?實作謅?rror Handling??

**???系統系統?**:

```typescript
try {
  const result = await client.generateESGReport(request);
} catch (error) {
  omniLogger.error('Report generation failed', { error });

  // ????謕??狗?砥????
  if (error.code === 'TIMEOUT') {
    // ?豯殷???系統?
  } else if (error.code === 'VALIDATION_ERROR') {
    // ?頦???功能
  }
}
```

---

### 3. SSOT ?賹??系統?

\*_??TypeScript ???????_:

```typescript
import { ThreePlusOneProtocolExecutor } from './UniversalComponentCore';

// Webhook ??謕??佇?????
const validated = ThreePlusOneProtocolExecutor.execute(webhookData, protocol);

if (!validated) {
  throw new Error('3+1 Protocol validation failed');
}
```

---

### 4. 實作???系統?

```bash
# .env
CREWAI_SERVICE_URL=http://localhost:8000
WEBHOOK_BASE_URL=https://api.esgss.com
CREWAI_API_KEY=your_api_key
JUNAIKEY_WEBHOOK_TOKEN=your_webhook_token
```

---

## ?? 系統?伍????

### ??謕?雓系統?

\*_系統?系統?核心?荒???_:

```bash
# Terminal 1: Python CrewAI 系統?
cd server/crewai
python -m uvicorn main:app --reload --port 8000

# Terminal 2: TypeScript ?豲????
npm run dev
```

### 系統系統?

\*_系統? 1: 實作?ㄜ????_

```
Nginx ??TypeScript (3000) ??Python (8000)
```

\*_系統? 2: ?蝞豢?????_

```
Load Balancer
實作??TypeScript Service (docker)
實作??Python CrewAI Service (docker)
```

---

## ?? ??謕?系統?

### 1. ??謕?系統?

```typescript
// ?豲狀????蛔?????????謅?頩??謕?實作豯券????
const execution = await client.generateESGReport(request);

// ?????系統? ID ???????
return { executionId: execution.executionId };

// 系統嚚???????
GET /api/crew/status/:executionId
```

```typescript
import { redis } from '../utils/redis';

const cacheKey = `crew:result:${executionId}`;
const cached = await redis.get(cacheKey);

if (cached) {
  return JSON.parse(cached);
}

// 實作豲????
const result = await client.getExecutionStatus(executionId);
await redis.setex(cacheKey, 3600, JSON.stringify(result));
```

---

## ???瞏?鞎播????

### TypeScript ??

- [x] ??渡?????????擳揚?謅?crewai.types.ts`??
- [x] ????謅??蟡?系統?`CrewAIClient.ts`??
- [x] Webhook ??謕??制????`crewai_receiver.ts`??
- [x] ??謕?系統?
- [x] 實作畾???

### Python ??

- [x] CrewAI ???實作?庥sg_crew.py`??
- [x] ??雓??謕????????
- [x] Webhook 系統?
- [x] ??謕?系統?

### 實作頦???

- [ ] ????殉???????
- [ ] 功能??∴???
- [ ] ??謕?核心??∴???

---

## ?? ?頛舀???船??謚叟?

### ??ㄞ?獢?頩????ㄜ??

```typescript
// 1. ??TypeScript 實作豲????
import { getCrewAIClient } from '../services/CrewAIClient';

export class ESGReportService {
  async createReport(organizationId: string) {
    const client = getCrewAIClient();

    // 系統? CrewAI 系統?
    const execution = await client.generateESGReport({
      organizationId,
      frameworks: ['GRI', 'SASB', 'TCFD'],
    });

    // 系統? A: ??謕??????
    const result = await client.waitForCompletion(execution.executionId);
    return result;

    // 系統? B: ??謕??????系統?
    return { executionId: execution.executionId };
  }

  async getReportStatus(executionId: string) {
    const client = getCrewAIClient();
    return await client.getReportStatus(executionId);
  }
}
```

```typescript
// 2. ??API ?豲?????????
app.post('/api/reports/generate', async (req, res) => {
  const { organizationId } = req.body;

  const service = new ESGReportService();
  const result = await service.createReport(organizationId);

  res.json(result);
});

app.get('/api/reports/status/:id', async (req, res) => {
  const service = new ESGReportService();
  const status = await service.getReportStatus(req.params.id);

  res.json(status);
});
```

---

<div align="center">

**TypeScript ? Python ? CrewAI**

**系統系統? ????渡??????????SSOT ?賹??**

2026-01-18

</div>
