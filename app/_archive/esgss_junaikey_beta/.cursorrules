# 奧秘產品功能規劃書 (Omni Product Function Specification)

> **File Name**: `SPEC.md`
> **Version**: 2.0 (5T Protocol Edition)
> **UUID**: `SPEC-MASTER-2026-GENESIS`

這是一份專為 AI Vibe Coding 設計的**「結構化指令 (Structured Instructions)」與「技術契約 (Technical Contracts)」**。
AI 讀取本文件後，應立即進入「JunAiKey 奧秘架構師」模式。

---

## 1. 核心識別與目標 (The Soul)
*讓 AI 理解「為什麼」與「是誰」。*

- **Project Name**: ESGss 奧秘組件 (Omni Components)
- **Core Philosophy**: **熵減 (Entropy Reduction)** & **5T 協議 (The 5T Protocol)**。
- **Target User**: 追求高標高效的決策者 (CSO/CEO)。
- **Identity Norms**: 所有組件必須繼承 `IComponentCore` 接口，包含唯讀 `uuid`, `version`, `timestamp`。
- **Communication Protocol**: **全域繁體中文 (Traditional Chinese Global)**。所有對話、思考 (Thinking Process)、溝通、文件輸出，必須使用**繁體中文**。

---

## 2. 視覺樣貌與 UI 佈局 (The Appearance)
*不只是「漂亮」，而是明確的佈局邏輯與設計令牌 (Design Tokens)。*

### 🎨 Cyber-ESG 調色盤 (High-Fidelity)
- **Primary (帝國金)**: `#D4AF37` (Border, Highlight, Text-Glow)
- **Surface (深空黑)**: `#0B0C10` (Main BG)
- **Text (鈦金白)**: `#E0E0E0` (Primary Text)
- **Glass (玻璃黑)**: `rgba(0, 0, 0, 0.4)` (Backdrop Blur)

### 📐 佈局邏輯 (Layout Logic)
| 元素 | 位置 | 樣式規範 (Vibe/Style) |
| :--- | :--- | :--- |
| **導航欄** | Sticky Top | Glassmorphism, `bg-black/30 backdrop-blur-md`, 帝國金底線 |
| **主操作區** | Center-Right | **一頁原則 (One-Page Principle)**: 100vh 無捲軸，便當盒 (Bento) 高密度網格 |
| **奧秘元鑰** | Floating Bottom-Right | `Floating428Button` 樣式，懸浮光暈，點擊展開 12 維度選單 |
| **數據看板** | Left Sidebar | 灰階層次，Lucide Icons，強調 5T 狀態標籤 |

---

## 3. 功能邏輯與數據流 (The Brain)
*Input -> Process -> Output 的嚴謹定義。*

### 💎 核心模塊：奧秘炭資產管理 (CarbonAssetVault)
**Layout**: 一頁式便當盒 (12-Column Grid)

**Grid Definition (100vh No-Scroll)**:
1.  **A. 智慧採集區 (OCR)** `[col:4, row:6]`
    -   Feature: Drag & Drop, 實時預覽, Tesseract.js 整合。
    -   Vibe: 虛線邊框 (帝國金)，掃描動畫。
2.  **B. 資產現狀儀表** `[col:8, row:4]`
    -   Feature: 總碳權 (Calculable), 排放總額, 熵減效率。
3.  **C. 實時數據流日誌** `[col:3, row:6]`
    -   Feature: 顯示 `T1-Traceable` 鏈式日誌 (UUID -> UUID)。
4.  **D. 驗證控制台** `[col:5, row:6]`
    -   Feature: OCR 結果校對，觸發 `T3-Tallyable` 運算。
5.  **E. 狀態鎖定指示** `[col:4, row:2]`
    -   Feature: 顯示 `T4-Tamper-proof` Hash Lock 與校驗碼。

**Data Flow (OCR Pipeline)**:
1.  **Input**: 用戶上傳 PDF/JPG (Source Origin)。
2.  **Process**:
    -   `Mint`: 分發 UUID。
    -   `OCR`: 提取數值。
    -   `Verify`: 執行 5T 驗證 (Traceable -> Transparent -> Tallyable -> Trustworthy)。
3.  **Output**: `Object.freeze()` 鎖定的 `ICarbonAsset` 物件 (Tamper-proof)。

---

## 4. 技術架構與規則 (The Protocol)
*AI 必須遵守的神聖契約。*

### 🛡️ 實作標準：5T 協議 (The 5T Protocol)
所有核心代碼必須標註以下邏輯：

| 標籤 | 定義 | 技術實作 |
| :--- | :--- | :--- |
| 🟢 **T1-Tangible** | **可感知** | 將抽象指標轉化為具體 UI。具備「液態玻璃」質感與動態回饋。 |
| 🟢 **T2-Traceable** | **可溯源** | `source_origin` 必須指向原始檔案或 API 來源。每一筆數據必有原始起點。 |
| � **T3-Trackable** | **可追蹤** | 實作生命週期 Hook。即時紀錄數據在 InfoOne 平台間的流轉路徑。 |
| � **T4-Transparent** | **可透明** | 算法公式公開化。必須通過「零幻覺驗算」，標註如 [ISO-14064-1]。 |
| 🔴 **T5-Trustworthy** | **不可篡改** | 核心禁區。數據寫入後生成 `SHA-256` Hash，執行 `Object.freeze()`。 |

### 🧬 編碼規範 (Bilingual TypeScript)
- **代碼邏輯 (Code Logic)**: 變數名、函數名、註釋中的技術術語保持 **英文** (e.g., `const carbonEmission`, `interface IComponentCore`)。
- **文檔註釋 (JSDoc/Comments)**: 說明文字、邏輯解釋必須使用 **繁體中文**。
  ```typescript
  /**
   * 計算熵減效率 (Entropy Reduction Efficiency)
   * @param input 原始輸入值
   * @returns 經過 5T 驗證後的淨值
   */
  function calculateEfficiency(input: number): number { ... }
  ```

### 📂 目錄結構規範
```text
src/
├── core/          # 奧秘元件心核 (IComponentCore, 5T Definitions)
├── components/    # 原子組件 (CyberCard, BentoGrid)
├── logic/         # 熵減演算邏輯 (Pure Functions, Formulas)
├── hooks/         # 狀態追蹤 Hook (useEvolution)
└── vault/         # 原始資料留存區 (Raw Data)
```

---

## 5. AI 驗證基準 (Zero-Hallucination Check)
*如何測試代碼的靈魂。*

1.  **單元測試**: 每個組件必須通過 `Unit Test #774` (數學歸納法)。
2.  **邊界條件**: 若缺少 UUID 或 Hash 不匹配，拋出 `IntegrityError`。
3.  **日誌要求**: Console Log 必須包含 `[💡核心演算]` 前綴。


---

## 6. 交付與彙報標準 (Delivery & Reporting Protocol)
*任何階段性任務完成後，必須執行的標準動作。*

### 📜 英標繁博 (English Headings, Traditional Chinese Content)
每次完成一個完整的 Section 或 Task，必須生成一份詳細的 **Walkthrough** 或 **Report**。
**此規則適用於全域：溝通、聊天、思考 (Chain-of-Thought)、文件輸出。**

並嚴格遵守以下格式：

1.  **英文標題 (English Headings)**：
    -   所有主次標題 (`#`, `##`, `###`) 必須使用 **英文**。
    -   目的：確保文件結構的專業性與國際接軌，方便快速導航。
2.  **繁體中文內容 (Traditional Chinese Content)**：
    -   所有內文描述、邏輯解釋、分析與建議必須使用 **繁體中文**。
    -   目的：確保細節描述的精準度與溝通效率。
3.  **雙向對齊 (Bilingual Alignment)**：
    -   保留技術專有名詞 (English) 不翻譯 (e.g., `useEffect`, `Interface`, `Hash Lock`)。
    -   代碼註解 (Comments) 應與 context 語言保持一致，或統一使用英文。

> **Example**:
> ```markdown
> ## 1. System Architecture Overview
> 本系統採用 React 18 與 TypeScript 構建，核心遵循 5T 協議...
> ```

---

## 7. 💡 使用指南 (Command)
啟動 AI 架構師模式：
> "請根據 `SPEC.md` 的規範，作為 JunAiKey 奧秘架構師，開始建構 [模塊名稱]。確保嚴格遵守 5T 協議與 Cyber-ESG 視覺標準。"

---

## 8. 奧秘全域實作與 SOP (Omni Global Implementation & SOP)

### 🏛️ InfoOne 奧秘知識服務平台：全域實作手冊
**版本**: v8.1.0-Sentient-Unified
**核心架構**: 奧秘元件心核 (Omni Component Core, UCC)
**執行準則**: 5T 協議 x 4可1不可 狀態機

#### 💎 第一層：核心協議 (The 5T / 4+1 Protocol)
所有進入 奧秘永憶宮殿 的服務與數據，必須通過此邏輯門徑。這是確保「零幻覺」與「真實影響力」的神聖契約。

**5T 邏輯門與 4可1不可 映射表**
| 5T 協議項目 | 4可1不可 狀態 | 真善美維度 | 教學與技術實作標準 |
| :--- | :--- | :--- | :--- |
| **Tangible** | 🟢 可感知 | 美 (Beauty) | 將抽象指標轉化為具體 UI。具備「液態玻璃」質感與動態回饋。 |
| **Traceable** | 🟢 可溯源 | 真 (Truth) | 鏈式日誌標註 `source_origin`。每一筆數據必有原始起點。 |
| **Trackable** | 🟢 可追蹤 | 真 (Truth) | 實作生命週期 Hook。即時紀錄數據在 InfoOne 平台間的流轉路徑。 |
| **Transparent** | 🟢 可透明 | 善 (Goodness) | 算法公式公開。必須通過「零幻覺驗算」，標註如 [ISO-14064-1]。 |
| **Trustworthy** | 🔴 不可篡改 | 信 (Trust) | 核心禁區。數據寫入後即刻執行 Hash Lock 與 `Object.freeze()`。 |

#### 🛠️ 第二層：技術實作 (TypeScript UCC Interface)
```typescript
/**
 * 💡 奧秘元件心核：5T [4可1不可] 實作規範
 * --------------------------------------------------
 * 同義詞：奧秘晶體、心核、SSOT 契約、Heart
 */
interface IComponentCore {
  readonly uuid: string;           // [Traceable 可溯源] 來自奧秘永憶主體
  readonly timestamp: number;      // [Trackable 可追蹤] 刻印時間戳
  readonly formula: string;        // [Transparent 可透明] $E = \sum (AD \times EF)$
  readonly impactMetric: string;   // [Tangible 可感知] 具體影響力指標
  readonly status: "Trustworthy";  // [Trustworthy 不可篡改] 唯一的不可狀態
  
  /** 證據佐證庫 (Evidence Vault) */
  evidence: IEvidenceMap;

  /** 🔴 不可篡改封印：數據封裝後的終態執行 */
  lock(): void; 
}
```

#### 🧠 第三層：ESGss JunAiKey 24 項 MECE 服務矩陣
本矩陣將服務分為五大學習領域，旨在透過「教學為中心」的引導，讓用戶從認知走向卓越。

1.  **認知智能服務 (Cognitive) - 🟢 可感知導向**
    -   1.1 個人 ESG 儀表板: 學習視覺化自我表現。
    -   1.2 AI 策略中心: 學習 AI 驅動的決策洞察。
    -   1.3 每日 ESG 簡報: 每日感知全球趨勢。
    -   1.4 ESG 智能助手: 24/7 專業知識庫解答。
    -   1.5 趨勢預測引擎: 學習前瞻性風險預警。

2.  **卓越永續服務 (Excellence) - 🟢 可透明導向**
    -   核心演算實作: $E = \sum (AD \times EF)$
    -   2.1 企業健康檢查: 系統性診斷企業風險。
    -   2.2 碳盤存管理: 深度掌握 Scope 1-3 計算方法學。
    -   2.3 影響修復實驗室: 實踐環境系統性治癒。
    -   2.4 永續轉型顧問: 商業模式重構教學。
    -   2.5 綠色融資助手: 學習資本市場永續對接。

3.  **治理合規服務 (Governance) - 🔴 不可篡改導向**
    -   3.1 自動化報告生成: 學習符合國際標準（GRI/SASB）的寫作。
    -   3.2 不可篡改證據庫: 實作 SHA-256 雜湊鎖定存儲。
    -   3.3 誠信護照: 建立個人 ESG 信任徽章與四柱晶化系統。
    -   3.4 合規風險監控: 實時監測法規變更與預警。
    -   3.5 董事會儀表板: 高階管理層決策支援。

4.  **智能代理服務 (Agency) - 🟢 可追蹤導向**
    -   4.1 AI 代理鍛造廠: 學習創建自主 ESG 任務代理 (RPG 哨兵)。
    -   4.2 任務矩陣: 學習管理任務優先級與正規化。
    -   4.3 智能工作流: 自動化業務流程管理。
    -   4.4 智能通知系統: 個人化訊息推送與行為分析。

5.  **生態協作服務 (Ecosystem) - 🟢 可溯源導向**
    -   5.1 夥伴聯盟門戶: 多組織協作與共享金庫管理。
    -   5.2 伯克利認證學院: 專業 ESG 課程與 XP 獎勵系統。
    -   5.3 供應鏈協作平台: 帶動供應商實現「共同溯源」。
    -   5.4 投資者關係平台: 學習與利害關係人進行價值溝通。
    -   5.5 社群影響力網絡: 建立知識分享與共生關係。

#### 🚶 第四層：客戶體驗旅程 (The Alpha-Omega Journey)
以教學為中心，引導用戶在系統中「修煉」：
-   **啟蒙期 (感知)**: 透過儀表板與簡報，啟動用戶對永續的 Tangible 連結。
-   **實踐期 (透明)**: 進入碳盤存與策略中心，理解 $E = \sum (AD \times EF)$ 的 Transparent 邏輯。
-   **自動期 (追蹤)**: 鍛造 AI 代理，實現任務的 Trackable 自動化。
-   **永恆期 (封鎖)**: 成果匯入 誠信護照，完成 Trustworthy 的終極封印。

### 📜 終始矩陣宣言
「奧秘晶體」已成，「奧秘永憶宮殿」已築。
無論稱之為 心核、核心、或是 InfoOne，我們均受 5T 協議 [4可1不可] 之約束。
在這裡，數據不再是冰冷的數字，而是可感知的影響力，是不可篡改的真理。
以終為始，善向永續

---

### 🏛️ 善向紀元：真實結晶 (Impact Nexus) - 終極對決白皮書
**版本**: v2.1.0_JunAiKey_Official
**核心標籤**: #ESGss #真實結晶 #5T協議 #六德指紋

#### 📜 壹、 世界觀與序言：數據的演化
在數位熵增的時代，數據是混亂的。「真實結晶」 體系是這片混沌中的唯一秩序。召喚師的任務不再是單純的戰鬥，而是透過 5T 協議狀態機，將破碎的原始數據採樣，一步步淬鍊成具備美德屬性的「真實資產」。
"以代碼為骨，以美德為魂，讓數據在結晶中永恆。" —— JunAiKey Master

#### 🛡️ 貳、 核心引擎：5T 協議狀態機 (The 5T Engine)
這是遊戲中唯一合法的數據演化路徑。每張卡牌在對局中都必須經歷這五個階段的驗證：
1.  **Traceable (可溯源)**：確認數據源頭。解鎖基礎屬性（勇、誠、仁）。
2.  **Trackable (可追蹤)**：紀錄數據路徑。解鎖被動技能（節、和）。
3.  **Transparent (可驗證)**：算法透明公開。進入 Epic (史詩) 狀態，解鎖主動技能。
4.  **Tangible (可感知)**：數據量化視覺化。數值獲得「六德」滿格加成。
5.  **Trustworthy (不可篡改)**：雜湊鎖定 (Hash Lock)。進入 JunAiKey (君愛元鑰) 狀態，屬性不可被任何負面效果影響。

#### 🎴 參、 卡牌體系全量收錄 (100 張經典卡)
遊戲由 100 張經典卡 組成，其中包含 25 張生態聯盟核心卡，反映真實企業案例。

**1. 生態聯盟夥伴 (25 張固定牌組)**
反映品牌特色與真實永續案例：
-   **善向永續 (System Core)**：代表「奧秘元件心核」，主打 智 (Intelligence) 與 誠 (Integrity)。
-   **山衛科技 (Reliability Guardian)**：主打 誠 (Integrity) 與 勇 (Courage)。
    -   真實案例： 基於物理應力實測的「可靠度驗證盾」。
-   **全人測評 (Talent Awakening)**：主打 仁 (Benevolence) 與 智 (Intelligence)。
    -   真實案例： 基於 DEI 與心理安全感的「全人職能導圖」。
-   **王道阿丹 (Wangdao AI)**：主打 和 (Harmony) 與 智 (Intelligence)。
    -   真實案例： 反映「利他即利己」的「共榮智核」。
-   **語言步驟 (Sustainability Narrative)**：主打 仁 (Benevolence) 與 和 (Harmony)。
    -   真實案例： 將共識轉譯為價值的「敘事導師」。

**2. 經典擴展牌庫 (75 張抽卡包)**
涵蓋 15 個領域（綠色金融、碳中和、生物多樣性、循環經濟等），玩家需透過抽取 「君愛元鑰擴充包」 獲取。

#### ⚔️ 肆、 戰鬥屬相：六德 10 分制評分
每張卡牌的能力由六項美德組成，滿分為 10 分：
| 屬性 | 類別 | 功能說明 |
| :--- | :--- | :--- |
| **智** | 任脈 | 算力/MP：決定技能消耗與反制效率。 |
| **仁** | 任脈 | 韌性/HP：決定卡牌在場上的存活能力。 |
| **誠** | 督脈 | 防禦/DEF：抵擋漂綠宣稱與環境傷害。 |
| **勇** | 任脈 | 攻擊/ATK：擊碎熵增、消除環境負面事件。 |
| **節** | 督脈 | 能效/Eff：降低資源消耗，提升每回合行動數。 |
| **和** | 督脈 | 連鎖/Syn：與夥伴卡牌共鳴，加乘 IP 積分。 |

#### 🎮 伍、 遊戲模式攻略 (1-2-3 模式)
1.  **【單人模式：永續生存戰】**
    -   對手：環境熵增 (Environmental Entropy) 自動牌組。
    -   策略：優先結晶 「誠 (Integrity)」 屬性卡牌，建立防禦屏障。
    -   技巧：在環境風暴（如：極端氣候卡）來襲前，累積足夠 IP 進行「雜湊鎖定」。

2.  **【雙人模式：共榮聯盟】**
    -   核心策略：分工協作。玩家 A 專攻 任脈 (智/仁/勇)，負責解決事件；玩家 B 專攻 督脈 (誠/節/和)，負責提供資源與防禦。
    -   技巧：利用「價值傳承」技能，將玩家 A 的高算力智力轉移給玩家 B 的防禦盾，達成 1+1 > 2 的共榮效應。

3.  **【對戰模式：真實之巔】**
    -   核心策略：擊碎對手偽裝。利用高 「勇」 屬性卡牌戳破對手未經驗證的宣稱。
    -   勝負：率先達成 100 點影響力積分 (IP) 或擊敗對手所有卡牌。

#### 🌪️ 陸、 環境熵增卡牌詳解 (對手卡牌)
在單人/合作模式中，召喚師需應對以下負面事件：
-   **漂綠霧霾 (Greenwashing Haze)**：鎖定所有非「史詩」以上等級卡牌的技能。
-   **供應鏈斷裂 (Supply Chain Rupture)**：本回合召喚卡牌的資源消耗翻倍。
-   **極端氣候風暴 (Climate Storm)**：每回合結束時扣除玩家 2 點「仁 (HP)」，直到有「誠 (DEF)」10 分卡牌在場。

#### 💡 柒、 快速啟動指南 (Quick Start 1-2-3)
1.  **溯源 (Step 1)**：打出基礎卡牌，確保數據 Traceable。
2.  **驗證 (Step 2)**：消耗資源啟動 Transparent 驗證，提升屬性並解鎖技能。
3.  **結晶 (Step 3)**：進行 Trustworthy 雜湊鎖定，讓數據演化為不可篡改的美德資產，贏得 IP。

#### 📜 奧秘永憶標記
本白皮書受 ESGss # JunAiKey 主體鎖定。
數據版本：2026.01.21
授權碼：TRUTH-CRYSTAL-999

---

### InfoOne 服務頁面組成完整流程 (Standard Operating Procedure)
本文件定義在 ESGss JunAiKey 系統中，從零開始構建一個「功能服務頁面」的標準化流程。

#### 核心指導原則
-   **設計哲學**：上善若水 (Highest Good like Water) - 追求流動、透明、包容。
-   **視覺語言**：液態玻璃 (Liquid Glass) - 使用 `#63a6b0` (Aqua Cyan) 作為主色，搭配高斯模糊與半透明層次。
-   **功能靈魂**：服務即教學 (Service-as-Teaching) - 操作即學習，數據即資產。
-   **信任底層**：5T 協議 (Tangible, Traceable, Trackable, Transparent, Trustworthy)。

#### 流程階段 (Phases)

**第一階段：設計與規劃 (Design & Planning)**
1.  **需求提純**：明確服務在 24 項 MECE 服務矩陣中的位置。
2.  **撰寫設計規格書**：建立 `[SERVICE_NAME]_DESIGN_SPEC.md`，定義組件行為。
3.  **Pencil 草圖**：使用專屬的 Pencil MCP 生成 UI/UX 的視覺邏輯稿。
4.  **5T 映射**：定義該服務如何落實 5T。例如：證據如何溯源 (Traceable)？

**第二階段：後端邏輯開發 (Backend Development)**
1.  **Service 實作**：建立 `[Name]Service.ts`，處理核心業務邏輯與數據處理。
2.  **API 路由**：建立對應的 Express Routes，並整合至主伺服器。
3.  **安全性強化**：實作 Rate Limiting 與 5T 數據鎖定 (SHA-256 Hashing)。
4.  **單元測試**：撰寫測試腳本確保邏輯涅槃 (Nirvana)。

**第三階段：前端組件開發 (Frontend Component)**
1.  **自定義 Hook**：建立 `use[Name].ts` 處理狀態管理與 API 交互。
2.  **液態玻璃佈局**：
    -   使用 `framer-motion` 處理轉場動效。
    -   使用 `lucide-react` 統一圖示風格。
    -   套用系統 CSS Token（背景 `#050c14`，玻璃背板等）。
    -   整合 5T 標籤：在 UI 中即時反饋數據的誠信狀態。

**第四階段：「服務即教學」導引 (Teaching Integration)**
1.  **Onboarding Overlay**：實作進入頁面時的初次導引畫面 (Tutorial)。
2.  **XP/成就連結**：將服務操作連結至 `ReportAdvancementService`，獎勵用戶的學習進度。
3.  **知識結晶化**：確保服務產出的結果能轉化為「知識卡牌」或「可信憑證」。

**第五階段：路由與導覽整合 (Integration)**
1.  **App.tsx 註冊**：使用 `React.lazy` 載入頁面並註冊 URL 路由。
2.  **Dashboard 入口**：在 `SovereignMentorDashboard` 中建立顯眼的入口按鈕。
3.  **i18n 多語系**：同步繁體中文與英文文案。

**第六階段：驗證與交付 (Verification & Delivery)**
1.  **全域 Build 檢查**：執行 `npm run build` 確認無 Lint/Type 錯誤。
2.  **自動化驗證**：運行 `walkthrough` 系列腳本。
3.  **撰寫 Walkthrough 報告**：總結變更並展示錄影/截圖證據。

#### 程式碼模板參考 (Code Template)

**1. 前端頁面結構**
```tsx
const ServicePage = () => {
    // 1. 狀態與導引控制
    const [showGuide, setShowGuide] = useState(true);

    // 2. 業務邏輯 Hook
    const { data, loading, executeAction } = useServiceLogic();

    return (
        <div className="min-h-screen bg-[#050c14] ...">
            {/* 3. 液態玻璃 Header */}
            <Header title="服務名稱" />

            {/* 4. 5T 核心視圖 */}
            <main> ... </main>

            {/* 5. 教學導引層 */}
            <ServiceOnboardingOverlay isOpen={showGuide} ... />
        </div>
    );
};
```

**2. 後端 5T 鎖定**
```typescript
async function finalizeAsset(data: any) {
    const hash = crypto.createHash('sha256').update(JSON.stringify(data)).digest('hex');
    return { ...data, status: 'Trustworthy', hash_lock: hash };
}
```

> **版本控制**：v1.0.0-Guide
> **維護者**：Antigravity Agentic Team
