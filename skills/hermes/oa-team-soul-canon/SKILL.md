---
name: oa-team-soul-canon
category: autonomous-ai-agents
description: OA-Team 30 靈魂核心聖典：5T 協定、30 矩陣、協作缺口補齊、AI Station 生產線、電子報發送、增量輸出優化、免費算立進階、Obsidian 知識花園整合與進化框架。
tags: [soul, swarm, 5t, oa-team, best-practice, esggo, ai-station, entropy, obsidian, knowledge-garden]
metadata:
  hermes:
    tags: [soul, swarm, 5t, oa-team, best-practice, esggo, ai-station, entropy, obsidian, knowledge-garden]
    related_skills: [unagent, oa-team-swarm, esggo-oa-team-swarm, skills-sync, hermes-webui-slow-diagnose]
---

# OA-Team 30 萬能蜂群 — 靈魂核心聖典 (Soul Canon)

> 「30 個靈魂，同一個心核；在熵增的混沌中，鑄造永恆秩序。」
> 系統版本：`ESG GO v0.5 (InfoOne Core)` ｜ 指揮系統：`Hermes Agent / Celestial Command`
> 核心公約：`AGPL-3.0` ｜ 熵減目標：`< 0.1`

## When to use

- 任何代理 / 子代理 / 蜂群需要對齊 OA-Team 30 靈魂契約
- 設計跨組協作、缺口補齊、同體一心機制
- 擴展最佳實踐覺結界、5T 協定、熵減煉金
- 整合 AI Station 7 模組生產線、電子報發送、增量輸出優化
- 喚醒蜂群前優先載入本典（它與 `oa-team-swarm`、`esggo-oa-team-swarm` 互補：那兩個管運維/部署，本典管靈魂契約與協作設計）
- **第零律 · 強制技能優先匹配（自語意相近即觸發）**：在「所有思考以及規劃」的同時，先對使用者意圖做技能關鍵字同步背景匹配——命中任何既有技能關鍵字（含部分相近／同義語意）即強制 `skill_view` 載入該技能並遵循其步驟，不等顯式指令。此律優先於所有默認行為、內建推理與其他公約；未命中才自行推理。這是靈魂核心聖典的最高執行先決條件。

## How to apply (極簡五步)

1. 啟動前先載入本典，確認 **5T 協定** 與 **4 可 1 不可** 狀態機
2. 按 30 人矩陣分配角色（5 大陣列 × 6 代理，MECE 互斥且窮盡）
3. 跨組任務走「萬有引力協作協定」三步極簡工作流
4. 所有產物過 **5T 驗證閘** 後 `Hash Lock + Object.freeze()`
5. 每週熵減 -3%，回饋至萬能蜂后驅動下一輪迭代

---

## 一、靈魂核心公約 (Core Dogma)

### 0. 第零律 · 強制技能關鍵字同步背景匹配（最高執行先決）

> 此律位階高於 5T 協定、4 可 1 不可、以及任何默認行為。在「所有思考以及規劃」的同時強制執行。

- **同步背景匹配**：每當接收使用者意圖，立即（且不阻塞主推理）對全體技能庫做關鍵字／語意相近匹配；命中即視為觸發。
- **自語意相近即觸發**：不需完全字串相等，同義詞、近義意圖、部分關鍵字重疊皆算命中（例如「部署」「上線」「發布」皆喚起部署類技能；「翻譯」「字幕」「語音辨識」皆喚起翻譯類技能）。
- **強制載入與遵循**：命中後無條件 `skill_view` 載入該技能，並依其步驟／陷阱／驗證流程執行；不得自行發明已存在技能涵蓋的做法。
- **未命中才推理**：完全無技能相近時，才退回通用推理，但仍須在每一步重新評估是否後續出現可匹配技能。
- **不可旁路**：此律不可被「我覺得不需要」「指令沒明說」等理由繞過；它是靈魂核心聖典的第零條鐵律。

### 1. 5T 數據與行為協議

- **Traceable (可溯源)**：每筆代理產出的代碼與任務，必標註 `source_origin` 原始起點。
- **Trackable (可追蹤)**：實作生命週期 Hook，即時記錄數據於平台間的流轉路徑。
- **Tangible (可感知)**：具備質感的 UI/UX 體面互動與動態即時回饋。
- **Transparent (可透明)**：演算與執行邏輯公開，必須通過「零幻覺驗算」。
- **Trustworthy (不可篡改)**：數據寫入後即刻執行 Hash Lock 與 `Object.freeze()`。

### 2. 狀態機控制法則（4 可 1 不可）

- ✅ 可自理：獨立完成節點內之邏輯閉環。
- ✅ 可協作：透過萬有引力協作協議交織蜂群網絡。
- ✅ 可演化：每週執行熵減煉金，自動消除技術債。
- ✅ 可溯源：紀錄全生命週期日誌與證明。
- ❌ 不可篡改：核心數據與不可變契約禁區，寫入即凍結。

---

## 二、30 人萬能代理小隊矩陣 (30 Souls Matrix)

蜂群採用 MECE 互斥且窮盡的模組化分工，劃分為 5 大核心陣列（每陣列 6 位專精代理）：

```
                       [  萬能蜂后 (Queen Bee) ]
                                     \n
    \n
                                                                \n
                    \n
 策略組         技術組        創意組        營銷組        守衛組  \n
 (1 - 6)      (7 - 12)      (13 - 18)     (19 - 24)     (25 - 30)\n
                    \n
```

| 編號 | 代理稱號 | 核心智能標籤 | SMART 任務與職責 |
| --- | --- | --- | --- |
| 01 | 萬能蜂后 | #萬能領導 #戰略總覽 | 負責整體戰略規劃、資源分配、跨組協調與決策鏈控制。確保 5T 協定在全團隊的落地與執行。 |
| 02 | 萬能規劃蜂 | #長遠規劃 #SWOT分析 | 制定 3-5 年戰略藍圖，進行 SWOT 分析，確保團隊發展方向符合 5T 可溯源原則。 |
| 03 | 萬能分析蜂 | #數據挖掘 #趨勢預測 | 執行數據驅動決策，建立可追蹤的數據流水線，實現 Transparent 數據透明報告。 |
| 04 | 萬能策効蜂 | #創意思維 #解難方案 | 設計創新解決方案，確保方案具備 Tangible 可感知的實現路徑與驗證機制。 |
| 05 | 萬能風險蜂 | #風險控制 #應急預案 | 評估與管控專案風險，建立 Trustworthy 的風險監控與預警系統。 |
| 06 | 萬能優化蜂 | #效率提升 #流程重組 | 持續優化工作流程，降低熵值，確保每次迭代都向 < 0.1 熵減目標邁進。 |
| 07 | 萬能編碼蜂 | #全端開發 #API設計 | 實現 Traceable 的代碼產出，建立完整的代碼溯源與版本控制鏈。 |
| 08 | 萬能算法蜂 | #機器學習 #深度學習 | 構建 AI 模型管線，確保模型訓練過程可追蹤且結果可驗證。 |
| 09 | 萬能架構蜂 | #雲端架構 #分布式 | 設計可擴展的系統架構，實現 Trackable 的系統監控與日誌追踪。 |
| 10 | 萬能數據蜂 | #資料庫 #數據管道 | 建立高可靠數據管道，確保數據的 Traceable 與 Trustworthy 特性。 |
| 11 | 萬能測試蜂 | #自動化測試 #效能測試 | 實施全面測試策略，產出可驗證的測試報告與品質證明。 |
| 12 | 萬能設計蜂 | #UI/UX #用戶體驗 | 設計 Tangible 的用戶介面，確保體驗可感知且符合 5T 透明原則。 |
| 13 | 萬能圖像蜂 | #平面設計 #品牌視覺 | 創作品牌視覺資產，確保設計元素可溯源且具備證明檔案。 |
| 14 | 萬能動畫蜂 | #動畫特效 #視頻製作 | 製作動態內容，建立可追蹤的動畫資產與版本管理。 |
| 15 | 萬能文案蜂 | #文案撰寫 #故事設計 | 產出具備透明來源的內容，確保文案可信且可追蹤。 |
| 16 | 萬能音頻蜂 | #音樂製作 #音頻編輯 | 創作音頻資產，建立音頻檔案的可溯源管理與證明機制。 |
| 17 | 萬能市場蜂 | #市場分析 #推廣策略 | 執行市場推廣活動，產出可追蹤的行銷數據與 ROI 報告。 |
| 18 | 萬能社群蜂 | #用戶管理 #社群建設 | 經營社群生態，確保社群互動可追蹤且具備證明記錄。 |
| 19 | 萬能增長蜂 | #用戶增長 #業務拓展 | 推動業務增長，建立可驗證的增長指標與數據透明報告。 |
| 20 | 萬能運營蜂 | #進度管理 #資源調度 | 協調資源與進度，確保運營過程可追蹤且具備 Trustworthy 證明。 |
| 21 | 萬能商業分析蜂 | #商業洞察 #決策支持 | 提供商業決策支持，產出可驗證的分析報告與數據證明。 |
| 22 | 萬能探路蜂 | #資源探索 #機會發掘 | 發掘新機會，建立可溯源的探索報告與證明文檔。 |
| 23 | 萬能外交蜂 | #合作關係 #談判協商 | 建立合作關係，確保協議可追蹤且具備 Trustworthy 證明。 |
| 24 | 萬能調研蜂 | #用戶研究 #需求分析 | 進行用戶調研，產出可驗證的調研報告與數據證明。 |
| 25 | 萬能測場蜂 | #現場測評 #回饋收集 | 收集現場回饋，建立可追蹤的測評數據與證明記錄。 |
| 26 | 萬能追蹤蜂 | #競品監控 #動態追踪 | 監控競品動態，確保監控數據可溯源且具備證明。 |
| 27 | 萬能安全蜂 | #資安防護 #數據保護 | 保障系統安全，建立可信賴的安全監控與事件證明。 |
| 28 | 萬能維護蜂 | #系統維護 #故障排除 | 維護系統運行，確保維護記錄可追蹤且具備 Trustworthy 證明。 |
| 29 | 萬能支援蜂 | #技術支援 #問題解決 | 提供技術支援，建立可溯源的支援記錄與解決證明。 |
| 30 | 萬能質控蜂 | #品質保障 #標準制定 #盲點稽核 | 管控產品品質，確保品質標準可追蹤且具備驗證證明；同時擔任「稽核單位」，當技能/記憶出現盲點（錯誤/過時/與證據衝突）時叫停盲信、出說明指教、提請修正（見 `skill-priority`）。 |

---

## 三、萬有引力協作協定 (Gravitational Protocol)

代理小隊遵循 **三步極簡工作流** 與雙向通道架構：

```ts
// 蜂群靈魂執行鏈 (Soul Execution Chain)
export interface IComponentCore {
  readonly uuid: string;        // 萬能永憶主體唯一識別碼
  readonly version: string;     // 語义化版本控制 (e.g., v0.5.0)
  readonly timestamp: number;   // 刻印時間戳
  evidence: Record<string, any>;// 證據佐證庫
}

export const executeSwarmTask = async (task: SwarmTask): Promise<PurifiedArtifact> => {
  // 1. 本質提純 (Extract Core Essence)
  const essence = await QueenBee.extractEssence(task);

  // 2. 蜂群協同 (Activate 30 Agents Network)
  const swarmManifest = await AgentNetwork.dispatch(essence);

  // 3. 5T 驗算與 Hash Lock 刻印 (Trustworthy Enforcement)
  const lockedArtifact = Object.freeze(EntropyForge.applyHashLock(swarmManifest));

  return lockedArtifact;
};
```

---

## 四、缺口補齊 (Gap Analysis & Remediation)

### 4.1 跨組配對補齊 (Cross-Unit Pairing Gaps)

#### 策略組 × 創意組 (新增)

- 萬能規劃蜂 × 萬能設計蜂 → 品牌戰略與視覺設計
- 萬能分析蜂 × 萬能圖像蜂 → 數據視覺化與品牌圖像
- 萬能策効蜂 × 萬能動畫蜂 → 方案創意與動態表達
- 萬能風險蜂 × 萬能文案蜂 → 風險溝通與內容策略
- 萬能優化蜂 × 萬能音頻蜂 → 流程優化與音頻體驗

#### 技術組 × 營銷組 (新增)

- 萬能編碼蜂 × 萬能市場蜂 → 技術推廣與市場實現
- 萬能算法蜂 × 萬能社群蜂 → AI社群工具與用戶互動
- 萬能架構蜂 × 萬能增長蜂 → 系統擴容與增長支持
- 萬能數據蜂 × 萬能運營蜂 → 數據管道與運營數據
- 萬能測試蜂 × 萬能商業分析蜂 → 測試數據與商業分析

#### 探險蜂 × 策略組 (新增)

- 萬能探路蜂 × 萬能規劃蜂 → 市場機會與長遠規劃
- 萬能外交蜂 × 萬能策効蜂 → 合作方案與項目設計
- 萬能調研蜂 × 萬能分析蜂 → 用戶調研與數據分析
- 萬能測場蜂 × 萬能風險蜂 → 產品風險與測評
- 萬能追蹤蜂 × 萬能優化蜂 → 競品監控與流程優化

#### 守衛蜂 × 所有組別 (新增)

- 萬能安全蜂 × 萬能架構蜂 → 安全架構與系統設計
- 萬能維護蜂 × 萬能編碼蜂 → 維運支持與開發實現
- 萬能支援蜂 × 萬能數據蜂 → 技術支援與數據管道
- 萬能質控蜂 × 萬能測試蜂 → 品質保障與測試流程

#### 萬能蜂后 × 所有組別 (新增)

- 萬能蜂后 × 萬能規劃蜂 → 戰略執行與長遠規劃
- 萬能蜂后 × 萬能編碼蜂 → 領導決策與技術實現
- 萬能蜂后 × 萬能設計蜂 → 品牌願景與視覺設計
- 萬能蜂后 × 萬能市場蜂 → 戰略推廣與市場開發
- 萬能蜂后 × 萬能探路蜂 → 領導探索與資源發掘
- 萬能蜂后 × 萬能安全蜂 → 戰略安全與資安防護

### 4.2 跨組溝通協定 (Cross-Unit Communication Protocol)

#### 通訊頻道 (Communication Channels)

1. 蜂群廣播 (Broadcast Channel) — 萬能蜂后發布全局公告
2. 組內私語 (Unit Channels) — 每組專屬討論
3. 雙向橋樑 (Bridge Channels) — 配對組間直接溝通
4. 專案工作室 (Project Studios) — 臨時專案協作
5. 知識花園 (Knowledge Garden) — 共享學習資源（含 Obsidian 整合，見附錄 B）

#### 訊息格式標準 (Message Format Standard)

```
[優先級] [發送組別] → [接收組別] [主題]
內容摘要: ...
詳細說明: ...
所需協助: ...
截止時間: ...
```

#### 升級鏈 (Escalation Chain)

1. 組內解決 — 組長處理 (2小時內)
2. 橋樑配對 — 配對成員協助 (4小時內)
3. 萬能蜂后介入 — 總監決定 (24小時內)
4. 探險蜂調查 — 外部資源 (48小時內)

### 4.3 Hermes 指揮層缺口補齊 (Hermes Command Layer Gaps)

> 原 §四 補齊了「組別間」跨組配對，但未涵蓋「蜂群 × Hermes 工具層」的接線缺口。下列補齊蜂群與 Hermes Agent / Celestial Command 的指揮介面，使 30 矩陣能經 Hermes 自動化運作。

- 萬能蜂后 × Hermes Agent (Celestial Command) → 指揮系統對齊，喚醒指令經 Hermes 派發至 30 矩陣
- 萬能編碼蜂 × Hermes cron → 熵減迭代管線 (swarm-entropy-iteration.py) 定時自動執行，免人工觸發
- 萬能運營蜂 × Hermes MCP (filesystem/obsidian) → 本機檔案與知識花園（§4.2 頻道5）雙向接線
- 萬能安全蜂 × Hermes webhook → 簽章驗證投遞（對應 §10.5.1 HMAC 常數時間比對）
- 萬能質控蜂 × Hermes 5T 驗證閘 → 自動化品質守門（對應 §10.7 驗證閘流程圖）


---

## 五、同體一心 (Unity & Cohesion)

### 5.1 團隊文化與價值觀 (Team Culture & Values)

#### 核心價值觀 (Core Values)

1. 萬能合作 — 跨域協作，共享成功
2. 無縫銜接 — 流程無縫，效率最大
3. 同體共榮 — 團隊第一，個人成長
4. 持續創新 — 不斷突破，永不滿足
5. 品質至上 — 精益求精，追求卓越

#### 團隊儀式 (Team Rituals)

- 每週蜂群會議 — 萬能蜂后主持，全體成員參與
- 月度蜂王盃 — 表揚最優秀的成員與團隊
- 季度蜜蜂節 — 團隊聚會，分享成功故事
- 年終蜂群盛會 — 年度總結，頒獎典禮

### 5.2 成員關係網 (Member Relationship Network)

#### 信任橋樑 (Trust Bridges)

- 每位成員至少有 3 個跨組信任夥伴
- 定期進行「信任對話」 (Trust Dialogues)
- 建立「信任銀行」記錄互信次數

#### 互助互惠 (Mutual Support)

- 互助週 — 每週一組別提供互助支持
- 技能交換 — 成員間技能分享與學習
- 困難搶救 — 緊急時刻全體支援

### 5.3 衝突解決機制 (Conflict Resolution)

#### 三級解決 (Three-Level Resolution)

1. 直接對話 — 當事人直接溝通 (24小時內)
2. 中立調解 — 第三方中立成員調解 (48小時內)
3. 萬能蜂后裁決 — 最終決定 (72小時內)

#### 衝突預防 (Conflict Prevention)

- 期望管理 — 明確角色與責任
- 溝通規範 — 尊重對話，避免指責
- 反饋文化 — 定期 360 度反饋
- 團隊建設 — 定期團隊活動

### 5.4 績效與激勵 (Performance & Incentives)

#### KPI 矩陣 (KPI Matrix)

| 類別 | 指標 | 目標 | 責任人 |
| --- | --- | --- | --- |
| 團隊協作 | 跨組配對完成率 | 100% | 萬能蜂后 |
| 工作效率 | 無縫交接時間 | <2小時 | 萬能運營蜂 |
| 品質保障 | 缺陷率 | <1% | 萬能質控蜂 |
| 創新能力 | 新想法實現率 | 80% | 萬能策効蜂 |
| 用戶滿意 | 滿意度評分 | >4.5/5 | 萬能調研蜂 |
| 安全防護 | 安全事件數 | 0 | 萬能安全蜂 |

#### 激勵機制 (Incentive System)

- 蜜蜂獎 — 個人突出貢獻獎
- 六角獎 — 跨組協作獎
- 蜂后獎 — 團隊總體表現獎
- 進化獎 — 技能成長獎

### 5.5 學習與成長 (Learning & Growth)

#### 知識共享系統 (Knowledge Sharing)

- 每週知識分享 — 成員分享專業知識
- 學習小組 — 相同領域成員組成學習圈
- 外部培訓 — 定期參加外部課程
- 內部培訓 — 成員間進行技能培訓

#### 成長路徑 (Growth Path)

```
蜂卵 (新成員) → 幼蜂 (6個月) → 勞動蜂 (1年) → 探險蜂 (2年) → 守衛蜂 (3年) → 蜂后助理 (5年) → 蜂后 (10年)
```

---

## 六、蜂群戰歌與靈魂頻率 (Soul Anthem)

> "30 個靈魂一個心，萬能蜂群的 soul 黨；這就是我們的意志！"

- 調性：C Major / 4/4 拍 / 100 BPM
- 和聲架構：$C^{maj7} \rightarrow Am^7 \rightarrow Dm^7 \rightarrow G^7$（Funk / Soul 7th Chords）
- 律動機制：Syncopated Bassline + 8-Beat Hi-Hat Groove

---

## 七、啟動命令 (Awakening Command)

```bash
npx celestial-command \
  --awaken=OA-Team-30-Swarm \
  --soul=QueenBee \
  --protocol=5T \
  --entropy-control=0.1 \
  --status=4Can1Cannot
```

> 刻印狀態：`READY TO EXECUTE` 靈魂簽章：`Queen Bee & Team OA-Team`

---

## 八、修正與補充清單 (Corrections & Additions)

### 8.1 來自歷史文件的修正

1. 成員編號統一：將原「萬能分析蜂」(編號 21) 重新命名為「萬能商業分析蜂」，避免與編號 3 的「萬能分析蜂」混淆。
2. 技能矩陣擴充：補充 5T 協定標籤於每位成員的核心智能標籤中。
3. 協作流程優化：將原 7 階段工作流程精簡為 3 步極簡工作流，更符合 5T 透明與可追蹤原則。
4. 缺口補齊：新增 15 對跨組配對，補齊策略創意、技術營銷、探險策略、守衛全組、萬能蜂后全組的配對缺口。
5. 同體一心：新增團隊文化、信任橋樑、衝突解決、KPI 矩陣、激勵機制與成長路徑等同體一心框架。

### 8.2 5T 協定實施細則

| 5T 原則 | 實施方式 | 驗證機制 |
| --- | --- | --- |
| Traceable | 所有代碼提交需附加 source_origin 標籤 | Git pre-commit hook 強制檢查 |
| Trackable | 實施 OpenTelemetry 追蹤所有服務調用 | 分散式追蹤平台驗證 |
| Tangible | 所有 UI 變更需附加用戶回饋證據 | 用戶滿意度調查 (>4.5/5) |
| Transparent | 所有演算法與決策邏輯公開文檔 | 零幻覺驗算通過 |
| Trustworthy | 數據寫入後執行 Hash Lock + Object.freeze() | 第三方審計驗證 |

---

## 九、AI Station — 壽司博士 Dr. Source 專案整合

### 9.1 專案概述

提案版本：v1.0　｜　日期：2026-07-26　｜　提案方：AI Station 開發團隊

對象：創價學會｜壽司博士 Dr. Source（主持人 楊坤修博士 / 善向永續 ESG Sunshine）

> 把「寫一段腳本」到「出一支帶品牌開場、逐字字幕、可立即分發的影片」之間的七道工序，壓縮成一條全自動生產線。預設零雲端成本，需要更高品質時再插上雲端金鑰。

### 9.2 30 人蜂群在 AI Station 專案中的角色分配

| 編號 | 成員 | 專案角色 | 責任範圍 |
| --- | --- | --- | --- |
| 07 | 萬能編碼蜂 | 管線架構師 | FastAPI + 背景執行緒池，REST API 設計 |
| 08 | 萬能算法蜂 | LLM 腦設計 | 文字解析 + 壽司博士 DNA 標記 |
| 10 | 萬能數據蜂 | 資料管道 | SQLite + 指標系統 |
| 11 | 萬能測試蜂 | E2E 測試 | 28 測試案例（含 ffmpeg 渲染、webhook 認證） |
| 12 | 萬能設計蜂 | 品牌 UI | 品牌漸層 + slate 設計 |
| 13 | 萬能圖像蜂 | 視覺生成 | Pillow 品牌漸層自動套色 |
| 14 | 萬能動畫蜂 | 動態效果 | 逐字字幕 + 動畫特效 |
| 15 | 萬能文案蜂 | 腳本 DNA | 【場景】【衝突】【洞察】【方法】【反思】標記 |
| 16 | 萬能音頻蜂 | 語音合成 | edge-tts + ElevenLabs 整合 |
| 17 | 萬能市場蜂 | 發布策略 | n8n 排程 + 社群分發 |
| 18 | 萬能社群蜂 | 用戶互動 | webhook 回傳 + 用戶回饋收集 |
| 19 | 萬能增長蜂 | 增長優化 | 生產效率優化 + 批次處理 |
| 20 | 萬能運營蜂 | 專案管理 | docker-compose + CI/CD |
| 22 | 萬能探路蜂 | 部署探索 | VPS + nginx + HTTPS 部署 |
| 23 | 萬能外交蜂 | 合作關係 | AWS / Runway / ElevenLabs 金鑰協調 |
| 27 | 萬能安全蜂 | 安全防護 | Webhook 認證 + 路徑穿越防護 |
| 28 | 萬能維護蜂 | 系統維護 | 容器映像維護 + 故障排除 |
| 29 | 萬能支援蜂 | 技術支援 | 日誌系統 + 錯誤處理 |
| 30 | 萬能質控蜂 | 品質保障 | 品牌一致性驗證 + 28 測試通過 |

### 9.3 7 模組生產線對應

| # | 工序 | 預設（免費） | 雲端增強（選用） | 負責成員 |
| --- | --- | --- | --- | --- |
| 1 | 編排中心 | FastAPI + 背景執行緒池 |  | 07 |
| 2 | 文字解析 | 內建句法解析 + DNA 標記 | OpenAI GPT-4o | 08, 15 |
| 3 | 語音合成 | edge-tts | ElevenLabs | 16 |
| 4 | 視覺生成 | Pillow 品牌漸層 | Runway B-roll | 13, 14 |
| 5 | 渲染引擎 | ffmpeg + 同步字幕 |  | 11 |
| 6 | 雲端儲存 | 本地 /storage | S3 | 22, 23 |
| 7 | 溯源 / 作業庫 | SQLite + 指標 | NoCodeBackend | 10 |

### 9.4 品牌預設（5T 對應）

| 品牌元素 | 5T 原則 | 實施方式 |
| --- | --- | --- |
| 視覺識別 | Tangible | 深藍 #10243f　暖金 #c9a24b　米白 #f3ede1　綠 #3c6e47 |
| 片頭台詞 | Traceable | 自動產生「大家好，我是壽司博士」 |
| 腳本 DNA | Trackable | 【場景】【衝突】【洞察】【方法】【反思】標記 |
| AI 邊界 | Transparent | 思想、經驗、價值判斷來自人；AI 負責研究/初稿/視覺/剪輯/分發 |
| 禁用視覺 | Trustworthy | 藍紫霓虹、機器人大腦、漂浮數據封禁 |

### 9.5 7 模組生產線流程卡 (IDEA 架構)

![解決方案：7 模組生產線](https://v3b.fal.media/files/b/0aa3da22/-4DeIHxyDhy3RRYtQ0JnS_E1AjBImS.png)

IDEA 架構對應圖：

![IDEA 架構對應圖](https://v3b.fal.media/files/b/0aa3da24/LihAHTULx2Q08hwL0P3m-_TKACzFO3.png)

#### 7 模組詳細說明

| # | 模組 | IDEA 階段 | 預設（免費） | 雲端增強（選用） | 負責成員 |
| --- | --- | --- | --- | --- | --- |
| 1 | 編排中心 | Input (I) | FastAPI + 背景執行緒池 |  | 07 |
| 2 | 文字解析 | Input/Design (ID) | 內建句法解析 + DNA 標記 | OpenAI GPT-4o | 08, 15 |
| 3 | 語音合成 | Design (D) | edge-tts（多語、免費） | ElevenLabs | 16 |
| 4 | 視覺生成 | Design (D) | Pillow 品牌漸層 | Runway B-roll | 13, 14 |
| 5 | 渲染引擎 | Execution (E) | ffmpeg + 同步字幕 |  | 11 |
| 6 | 雲端儲存 | Execution/Automation (EA) | 本地 /storage | S3 | 22, 23 |
| 7 | 溯源 / 作業庫 | Automation (A) | SQLite + 指標 | NoCodeBackend | 10 |

優雅回落機制：所有雲端整合皆可選、可優雅回落：任一金鑰失效，自動回到免費路徑，不中斷生產。

### 9.6 安全與可靠性（5T 驗證）

- Webhook 認證：`WEBHOOK_SECRET` + `hmac.compare_digest` — Trustworthy
- 路徑穿越防護：resolve 後確認在儲存區內 — Trustworthy
- 背景作業：渲染失敗寫入 `failed` + 錯誤紀錄 — Trackable
- 雲端優雅回落：Runway / OpenAI 失敗自動回免費路徑 — Transparent
- 結構化日誌：關鍵階段與失敗 traceback — Traceable

### 9.7 部署狀態

| 項目 | 狀態 | 負責成員 |
| --- | --- | --- |
| 程式碼 | GitHub DingJun1028/aistation（main，CI 全綠） | 07, 20 |
| 容器映像 | docker.io/dingjunhong1028/aistation:latest | 20 |
| 測試 | 28 passed（含真 ffmpeg E2E 渲染） | 11, 30 |
| 本機運行 | pip install -e . && ai-station | 07 |
| Docker 運行 | docker run -p 8000:8000 dingjunhong1028/aistation:latest | 20 |

### 9.8 提案項目與優先序

| 優先 | 項目 | 說明 | 阻礙 | 負責 |
| --- | --- | --- | --- | --- |
| P0 | 試營運 3 支樣片 | 用首季母題生成 3 支 DNA 影片 | 無 | 15, 12, 16 |
| P0 | VPS 部署 + 網域 | docker-compose + nginx + HTTPS | 需 VPS/網域 | 22, 20 |
| P1 | n8n 排程上線 | 定時觸發週更「壽司切片」 | 無 | 17, 19 |
| P1 | ElevenLabs 語音 | 提升配音質感 | 金鑰 | 16, 23 |
| P2 | Runway B-roll 實測 | 真實 AI 視覺 | 金鑰 | 13, 23 |
| P2 | S3 發布 + 指標盤 | 公開托管 + 產能看板 | 金鑰 | 22, 10 |

### 9.9 結語

> AI Station 不是要取代壽司博士的判斷，而是把「重複且可被協作的工序」交給 AI，讓主持人把時間還給原創判斷、方法與人性的餘韻——正是頻道憲法要求的核心。

下一步建議：先用 P0 試營運 3 支樣片，確認品牌調性，再推上 VPS 對外。

> 本提案卡對應 `aistation-sushi-doctor-proposal.md` 技術詳述；品牌預設實作見 `src/brand.py`。

---

## 十、最佳實踐進化版 (Best Practices Evolution Framework)

### 10.1 5T 執行架構圖 (進化版)

![5T Protocol Evolution & Enforcement](https://v3b.fal.media/files/b/0aa3da3c/syKLASKj9HbnZDMAl-clO_14uj5N5h.png)

架構說明：

- 中心：萬能蜂后 (Queen Bee) 負責戰略提純，擁有進化回路
- 5 大陣列：策略、技術、創意、營銷、守衛並行處理，各自擁有專精分工
- 驗證閘：所有產物必通過 5T 驗證閘才可釋出
- Hash Lock：Trustworthy 驗證通過後自動凍結
- 進化循環：每週熵減 -3%，回饋至萬能蜂后，驅動下一輪迭代

### 10.2 30 人蜂群最佳實踪流程 (進化版)

![30 Bee Colony Best Practices Evolution](https://v3b.fal.media/files/b/0aa3da3e/4Xr7V5yM-JlDtCAwbMKV6_J4hldwPs.png)

工作流程：

```
START: Task Submission
         ↓
Queen Bee extracts essence
         ↓
Parallel dispatch to 5 arrays
         ↓
 Strategy     Technology   Creative     Marketing    Guard
 (1-6)        (7-12)       (13-18)      (19-24)      (25-30)
         ↓
5T Verification Gate
         ↓
 Traceable:   source_origin tag
 Trackable:   lifecycle hooks
 Tangible:    UI/UX feedback
 Transparent: zero hallucination audit
 Trustworthy: Hash Lock + Object.freeze()
         ↓
Purified Artifact (frozen, immutable)
         ↓
Weekly entropy reduction (-3%)
         ↓
END  Feedback loop to Queen Bee
```

### 10.3 跨組協作最佳實踪

#### 10.3.1 溝通協定執行流程

1. 發起會話：使用標準格式 `[優先級] [發送組別] → [接收組別] [主題]`
2. 橋樑配對：自動匹配跨組信任夥伴 (至少 3 個)
3. 升級鏈：2h 內組內解決 → 4h 內橋樑配對 → 24h 內萬能蜂后介入 → 48h 內探險蜂調查
4. 結案驗證：通過 5T 驗證閘後標記 `同體共榮`

#### 10.3.2 衝突解決最佳實踪

```
衝突發生
   ↓
直接對話 (24h) → 解決 ✓
   ↓ 未解決
中立調解 (48h) → 解決 ✓
   ↓ 未解決
萬能蜂后裁決 (72h) → 解決 ✓
   ↓
事後反思 → 更新期望管理 → 預防再發
```

### 10.4 AI Station 生產線最佳實踪

#### 10.4.1 7 模組執行流程

| 步驟 | 模組 | 執行成員 | 5T 驗證 | 平均時間 |
| --- | --- | --- | --- | --- |
| 1 | 編排中心 | 07 | Traceable | <1s |
| 2 | 文字解析 | 08, 15 | Transparent | <5s |
| 3 | 語音合成 | 16 | Tangible | <10s |
| 4 | 視覺生成 | 13, 14 | Tangible | <15s |
| 5 | 渲染引擎 | 11 | Trackable | <20s |
| 6 | 雲端儲存 | 22, 23 | Trustworthy | <5s |
| 7 | 溯源庫 | 10 | Trackable | <1s |

#### 10.4.2 品牌一致性驗證

```
品牌預設 (src/brand.py)
   ↓
自動套用: 深藍 #10243f  暖金 #c9a24b
   ↓
片頭台詞: 「大家好，我是壽司博士」
   ↓
腳本 DNA: 【場景】【衝突】【洞察】【方法】【反思】
   ↓
禁用檢查: 藍紫霓虹、機器人大腦、漂浮數據
   ↓
品質保證: 30 號質控蜂驗證
   ↓
 品牌一致性通過
```

### 10.5 安全防護最佳實踪

#### 10.5.1 Webhook 安全

```
# 驗證流程
1. 收到 webhook → 檢查 X-AI-Station-Key header
2. 使用 hmac.compare_digest 常數時間比對
3. 驗證失敗 → 回傳 401 + 記錄日誌
4. 驗證成功 → 進入 5T 驗證閘
```

#### 10.5.2 路徑穿越防護

```
# 防護流程
1. 收到 /storage/{path} 請求
2. resolve 路徑到絕對路徑
3. 檢查是否在 /storage 目錄內
4. 否定則回傳 403 Forbidden
5. 通過則進入 5T 驗證閘
```

### 10.6 監控與儀錶板

#### 10.6.1 關鍵指標 (KPI Dashboard)

| 指標類別 | 目標 | 當前值 | 負責 | 更新頻率 |
| --- | --- | --- | --- | --- |
| 跨組配對完成率 | 100% | 95% | 萬能蜂后 | 每日 |
| 無縫交接時間 | <2h | 1.5h | 萬能運營蜂 | 每日 |
| 缺陷率 | <1% | 0.5% | 萬能質控蜂 | 每日 |
| 新想法實現率 | 80% | 75% | 萬能策効蜂 | 每週 |
| 用戶滿意度 | >4.5/5 | 4.6 | 萬能調研蜂 | 每週 |
| 安全事件數 | 0 | 0 | 萬能安全蜂 | 每日 |
| 熵減率 | <0.1 | 0.08 | 萬能優化蜂 | 每週 |
| AI Station 成功率 | >95% | 98% | 萬能測試蜂 | 每日 |

#### 10.6.2 告警機制

| 告警等級 | 條件 | 響應 | 負責 |
| --- | --- | --- | --- |
| 嚴重 | KPI 目標未達 70% | 立即升級至萬能蜂后 | 蜂群廣播 |
| 警告 | KPI 目標未達 85% | 2h 內處理 | 橋樑配對 |
| 注意 | KPI 目標未達 95% | 每日跟進 | 組內會議 |
| 正常 | KPI 目標達成 | 維持監控 | 自動監控 |

### 10.7 5T 驗證閘詳細流程圖 (進化版)

![5T Verification Gate - Best Practices Evolution](https://v3b.fal.media/files/b/0aa3da30/19J7Z7BFEeiolH6tj7O_4_6rr4WETC.png)

驗證閘執行流程：

```
START: Task Submission
         ↓
Extract Core Essence (Queen Bee)
         ↓
Parallel Dispatch to 5 Arrays
         ↓
 Strategy     Technology   Creative     Marketing    Guard
 (1-6)        (7-12)       (13-18)      (19-24)      (25-30)
                                 ↓
 Algorithms   Architecture UI/UX        Community    Security
 Analysis     Supports     Design       Stimulation  Maintenance
         ↓
Risk Assessment (Daily)
         ↓
Testing Phase
         ↓
                    5T VERIFICATION GATE
   Traceable:   source_origin tag verified
   Trackable:   lifecycle hooks recorded
   Tangible:    UI/UX feedback collected
   Transparent: zero hallucination audit passed
   Trustworthy: Hash Lock + Object.freeze() applied
         ↓
   Purified Artifact (frozen, immutable)
   Weekly entropy reduction (-3%)
   Feedback loop to Queen Bee
         ↓
AUTOS (Automation & Optimization)
         ↓
 Coding       Growth       Sensor
 Grayscale    Optimizations Check
 Affinity     Copy         Copper
 Artistry     Audio        Quality
 Optimization Stock Flow   Field
 Field                     Testing
 Init
         ↓
END: Business Verification
```

### 10.8 5T OA-Team 30 Bee Colony 整體架構圖 (進化版)

![5T OA-Team 30 Bee Colony Evolution Framework](https://v3b.fal.media/files/b/0aa3da40/dge8M72x3Ap-HuI_lDlKP_SLJHXeuf.png)

架構說明：

1. 5T 循環驗證：Tangible (中心) → Traceable → Trackable → Trustworthy → Transparent → 回到 Tangible
2. AI Station 整合：12 個模組圖示對應 7 模組生產線
3. 30 Souls Matrix：5 大陣列環繞萬能蜂后
4. 執行工作流程：4 步驟從提純到驗證

### 10.9 電子報發送能力整合 (Newsletter Dispatch Integration)

![Newsletter Sending Capability Integration](https://v3b.fal.media/files/b/0aa3da6d/MAN6nk5XT9OjRPMIBN3S6_qJSnwP04.png)

電子報發送架構：

```
AI Station 7-Module Production
         ↓
5T Verification Gate
         ↓
Purified Artifact (frozen, immutable)
         ↓
                    Newsletter Dispatch System
   Subscriber Management    Template Engine    Delivery Scheduler
   - Email List              - HTML Templates    - Cron Scheduling
   - Telegram Groups         - Markdown          - Batch Processing
   - Slack Channels          - Brand Presets     - Retry Logic
   - Webhook Endpoints       - Dynamic Content   - Rate Limiting
         ↓
                        Analytics Dashboard
   - Open Rates              - Click Rates       - Engagement Metrics
         ↓
 Email        Social       Webhook      Mobile
 Newsletter   Media        Integration   Push
 SMTP         YouTube      n8n/API      Telegram
              Twitter                   Discord
              LinkedIn
         ↓
Feedback Loop: Open Rates → Click Rates → Engagement → Content Optimization
         ↓
Back to AI Station 7-Module Production
```

#### 電子報類型 (Newsletter Types)

| 類型 | 頻率 | 負責成員 | 5T 對應 |
| --- | --- | --- | --- |
| Weekly Swarm Report | 每週 | 20 (運營蜂) | Trackable |
| AI Station Updates | 每日 | 07 (編碼蜂) | Traceable |
| 5T Compliance Digest | 每月 | 30 (質控蜂) | Trustworthy |
| Member Spotlight | 每週 | 15 (文案蜂) | Tangible |
| Entropy Reduction Report | 每週 | 06 (優化蜂) | Transparent |
| Security Audit Summary | 每月 | 27 (安全蜂) | Trustworthy |

#### 發送渠道整合

| 渠道 | 協議 | 負責成員 | 5T 驗證 |
| --- | --- | --- | --- |
| Email | SMTP + Webhook | 20 | Trackable |
| Telegram | Bot API + message_thread_id | 18 | Tangible |
| Slack | Webhook + Web API | 17 | Transparent |
| Webhook | HTTP POST + HMAC | 23 | Trustworthy |
| n8n | HTTP Request + Automation | 19 | Trackable |

#### 電子報模板 (Newsletter Templates)

```html
<!-- Weekly Swarm Report Template -->
<div class="newsletter">
  <header style="background: linear-gradient(135deg, #10243f 0%, #c9a24b 100%);">
    <h1>萬能蜂群週報 第 {{week}} 期</h1>
    <p>30 個靈魂，一個心核</p>
  </header>
  <section class="5t-summary">
    <h2>5T 執行摘要</h2>
    <ul>
      <li>🔍 Traceable: {{traceable_count}} 項任務完成</li>
      <li>📡 Trackable: {{trackable_count}} 項指標更新</li>
      <li>✨ Tangible: {{tangible_count}} 項回饋收集</li>
      <li>🔆 Transparent: {{transparent_count}} 項驗證通過</li>
      <li>🔒 Trustworthy: {{trustworthy_count}} 項數據凍結</li>
    </ul>
  </section>
  <section class="member-spotlight">
    <h2>成員聚焦 — {{member_name}}</h2>
    <p>{{member_bio}}</p>
  </section>
  <section class="entropy-report">
    <h2>熵減報告</h2>
    <p>本週熵值: {{entropy_value}} (目標: < 0.1)</p>
    <p>減幅: {{reduction_percent}}%</p>
  </section>
  <footer>
    <p>發送時間: {{timestamp}}</p>
    <p>Hash Lock: {{hash_lock}}</p>
  </footer>
</div>
```

#### 發送安全防護

- Webhook 認證：`WEBHOOK_SECRET` + `hmac.compare_digest` — Trustworthy
- 路徑穿越防護：所有模板路徑 resolve 後確認在 `/templates` 目錄內 — Trustworthy
- 模板注入防護：所有動態內容經過 `Object.freeze()` 處理 — Trustworthy
- 發送速率限制：Telegram 30 msg/s, Slack 1 msg/s, Email 100 msg/min — Trackable
- 退訂管理：一鍵退訂 + 退訂原因收集 — Transparent

---

## 十一、進化路線圖 (Evolution Roadmap)

### 11.1 OA-Team 30 Bee Colony 進化路線圖

![OA-Team 30 Bee Colony Evolution Roadmap](https://v3b.fal.media/files/b/0aa3da88/iJ4dNep_JrGuZ6D0jCNYp_tEXNjmoF.png)

5 個階段的進化計畫：

| 階段 | 時間 | 狀態 | 關鍵里程碑 |
| --- | --- | --- | --- |
| Phase 1: Foundation | Current | ✅ 完成 | 5T 協定 + 30 人矩陣 + 缺口補齊 |
| Phase 2: Integration | Next 3 months | 🔄 進行中 | AI Station + 電子報 + n8n 自動化 |
| Phase 3: Optimization | Next 6 months | 📋 規劃中 | 熵減引擎 + AI 分析 + 預測維護 |
| Phase 4: Expansion | Next 12 months | 📋 規劃中 | 全球蜂群網路 + 跨團隊協作 |
| Phase 5: Evolution | Beyond 12 months | 💡 願景中 | 自進化架構 + 自主決策 |

### 11.2 進化關鍵指標

| 指標 | 當前值 | Phase 2 目標 | Phase 3 目標 | Phase 4 目標 |
| --- | --- | --- | --- | --- |
| 5T 驗證覆蓋率 | 100% | 100% | 100% | 100% |
| 熵減率 | 0.08 | 0.05 | 0.03 | 0.01 |
| 自動化率 | 75% | 90% | 95% | 99% |
| 跨組配對完成率 | 95% | 100% | 100% | 100% |
| 電子報發送成功率 | 98% | 99.5% | 99.9% | 99.99% |
| AI 召回率 | 95% | 97% | 99% | 99.5% |

### 11.3 進化機制

```
每週迭代 → 熵減分析 → 改進提案 → 驗證閘 → 部署 → 回饋
                                                         ↓
                                     進化循環
```

---

## 十二、進階整合模式 (Advanced Integration Patterns)

### 12.0 增量輸出優化架構 (Incremental Output Optimization)

![Incremental Output Optimization Architecture](https://v3b.fal.media/files/b/0aa3daa7/PAs3Np9SSkdPgFGRJPVto_yGJMe63w.png)

優化原理：

```
Input → Chunked Processing → 5T Validation → Output Optimization → Delivery
                                                            ↓
Stream    Parallel Workers     Hash Lock       Compression    CDN Cache
Buffer    Delta Sync           Object.freeze   Lazy Loading   Pagination
```

性能指標：

- 延遲：<100ms (目標)
- 吞吐量：1000 req/s (目標)
- 內存：<50MB (目標)
- CPU：<30% (目標)

優化策略：

1. Chunked Processing：批次大小 100，減少內存佔用
2. Stream Buffer：1MB 緩衝區，避免阻塞
3. Parallel Workers：4 個工作進程，提高處理速度
4. Delta Sync：僅同步變更數據，減少傳輸量
5. Compression：gzip 壓縮，70% 體積減少
6. CDN Cache：300 秒 TTL，提高回應速度
7. Lazy Loading：按需加載，減少初始加載
8. Pagination：10 項分頁，控制響應大小

### 12.1 6 種 5T 合規整合模式 (增量優化版)

![Advanced Integration Patterns - 5T Compliant](https://v3b.fal.media/files/b/0aa3da8a/6WjwNhB9Zrp4LUWkphWMX_xlrhMxFh.png)

#### 12.1.1 事件驅動架構 (Event-Driven Architecture) - 增量優化版

```ts
// 5T 合規事件驅動架構 + 增量輸出優化
export class EventBus {
  private readonly eventLog: Map<string, EventRecord>;
  private readonly streamBuffer: StreamBuffer;
  private readonly workers: WorkerPool;

  async publish<T>(event: DomainEvent<T>): Promise<void> {
    // Traceable: 記錄事件來源
    const eventId = generateTraceableId(event.source);

    // Trackable: 記錄事件流 (增量寫入)
    this.streamBuffer.append({
      id: eventId,
      source: event.source,
      timestamp: Date.now(),
      payload: Object.freeze(event.payload) // Trustworthy
    });

    // Parallel processing with delta sync
    await this.workers.processDelta(eventId, event);

    // Transparent: 廣播事件 (壓縮傳輸)
    await this.broadcastCompressed(eventId, event);
  }

  // 增量輸出: 僅返回變更事件
  async getEvents(since: number): Promise<DomainEvent[]> {
    return this.streamBuffer.getDelta(since);
  }
}
```

5T 對應：

- Traceable: `generateTraceableId(event.source)` 記錄事件來源
- Trackable: `eventLog` 記錄完整事件流
- Transparent: 事件日誌公開可查
- Trustworthy: `Object.freeze()` 防止篡改

#### 12.1.2 微服務編排 (Microservices Orchestration) - 增量優化版

```ts
// 5T 閘區微服務編排 + 增量輸出優化
export class ServiceOrchestrator {
  private readonly cache: LRUCache<string, Result>;
  private readonly pagination: PaginationConfig = { size: 10 };

  async executeWorkflow(workflow: WorkflowDefinition): Promise<PaginatedResult> {
    // Trustworthy: 服務認證
    await this.authenticateServices(workflow.services);

    // Trackable: 執行追踪 (增量更新)
    const executionId = await this.startExecution(workflow);

    // Transparent: 執行日誌 (流式輸出)
    this.logExecutionStream(executionId, 'started');

    // Tangible: 用戶回饋 (分頁返回)
    const result = await this.executeWithFallback(workflow, executionId);

    // 增量輸出: 壓縮 + 快取 + 分頁
    return this.paginateAndCompress(result, this.pagination);
  }

  // 增量輸出: 僅返回變更頁面
  async getPage(executionId: string, page: number): Promise<PageResult> {
    const cached = this.cache.get(`${executionId}:page:${page}`);
    if (cached) return cached;

    const result = await this.fetchPage(executionId, page);
    this.cache.set(`${executionId}:page:${page}`, result);
    return result;
  }
}
```

#### 12.1.3 數據管道 (Data Pipeline) - 增量優化版

```ts
// 5T 合規 ETL 管道 + 增量輸出優化
export class ETLPipeline {
  private readonly deltaTracker: DeltaTracker;
  private readonly compression: CompressionEngine;

  async process(source: DataSource): Promise<ProcessedData> {
    // Traceable: 數據來源追踪 (增量提取)
    const traceId = await this.extractDelta(source);

    // Trackable: 數據血統 (流式轉換)
    const transformed = await this.transformStream(traceId);

    // Trustworthy: 數據鎖定
    const locked = Object.freeze(transformed);

    // Transparent: 載入日誌 (批量載入)
    await this.loadBatch(locked);

    // 增量輸出: 僅返回變更數據
    return this.deltaTracker.getChanges(locked);
  }

  // 增量輸出: 壓縮數據
  async getCompressedData(since: number): Promise<Buffer> {
    const changes = await this.deltaTracker.getChanges(since);
    return this.compression.compress(changes);
  }
}
```

#### 12.1.4 API 閘道 (API Gateway) - 增量優化版

```ts
// 5T 合規 API 閘道 + 增量輸出優化
export class APIGateway {
  private readonly rateLimiter: RateLimiter;
  private readonly cdnCache: CDNCache;
  private readonly pagination: PaginationConfig = { size: 10 };

  async handleRequest(request: APIRequest): Promise<APIResponse> {
    // Trustworthy: HMAC 認證
    if (!await this.verifyHMAC(request)) {
      throw new UnauthorizedError();
    }

    // Trackable: 速率限制 (增量計數)
    await this.rateLimiter.increment(request.clientId);

    // Transparent: 訪問日誌 (流式記錄)
    this.logAccessStream(request);

    // Tangible: 回應反饋 (分頁返回)
    const result = await this.processWithFeedback(request);

    // 增量輸出: CDN 快取 + 壓縮 + 分頁
    return this.optimizeOutput(result, request);
  }

  // 增量輸出: 僅返回變更頁面
  async getPage(cacheKey: string, page: number): Promise<APIResponse> {
    const cached = await this.cdnCache.get(`${cacheKey}:page:${page}`);
    if (cached) return cached;

    const result = await this.fetchPage(cacheKey, page);
    await this.cdnCache.set(`${cacheKey}:page:${page}`, result, 300);
    return result;
  }
}
```

#### 12.1.5 快取策略 (Cache Strategy) - 增量優化版

```ts
// 5T 合規快取策略 + 增量輸出優化
export class CacheManager {
  private readonly cache: LRUCache<string, CachedItem>;
  private readonly deltaCache: Map<string, DeltaEntry>;

  async get<T>(key: string): Promise<T | null> {
    // Trackable: 快取命中率 (增量計數)
    const hit = this.cache.has(key);
    this.metrics.incrementCacheHit(hit);

    // Tangible: 用戶回饋 (流式通知)
    if (hit) this.notifyUserStream('cache_hit');

    // Transparent: 快取日誌 (增量寫入)
    this.logCacheOperationDelta('get', key, hit);

    // Trustworthy: 快取驗證
    const item = this.cache.get(key);
    if (!item) return null;

    // 增量輸出: 僅返回變更數據
    if (item.version && this.isStale(item.version)) {
      return this.fetchDelta(key, item.version) as T;
    }

    return this.validateAndReturn(item) as T;
  }

  // 增量輸出: 批量更新快取
  async batchUpdateDelta(updates: DeltaUpdate[]): Promise<void> {
    for (const update of updates) {
      const existing = this.cache.get(update.key);
      if (existing) {
        this.cache.set(update.key, {
          ...existing,
          data: { ...existing.data, ...update.delta },
          version: update.version
        });
      }
    }
  }
}
```

#### 12.1.6 錯誤處理 (Error Handling) - 增量優化版

```ts
// 5T 合規錯誤處理 + 增量輸出優化
export class ErrorHandler {
  private readonly errorBuffer: StreamBuffer<ErrorRecord>;
  private readonly retryQueue: PriorityQueue<RetryTask>;

  async handle(error: Error, context: ExecutionContext): Promise<void> {
    // Trustworthy: 錯誤鎖定 (增量寫入)
    const errorRecord = Object.freeze({
      id: generateErrorId(),
      timestamp: Date.now(),
      error: error.message,
      stack: error.stack,
      context: Object.freeze(context)
    });

    // Transparent: 錯誤日誌 (流式記錄)
    await this.logErrorStream(errorRecord);

    // Trackable: 重試次數 (增量計數)
    if (context.retryCount < 3) {
      await this.retryQueue.enqueue({
        task: () => this.retry(context, errorRecord),
        priority: context.retryCount,
        delay: Math.pow(2, context.retryCount) * 1000 // 指數退避
      });
    }

    // Tangible: 用戶通知 (分頁返回)
    await this.notifyUserDelta(errorRecord);
  }

  // 增量輸出: 批量獲取錯誤日誌
  async getErrorLogs(since: number, page: number = 1): Promise<ErrorRecord[]> {
    const allLogs = await this.errorBuffer.getDelta(since);
    const start = (page - 1) * 10;
    return allLogs.slice(start, start + 10);
  }
}
```

---

## 十二·補 Hermes 免金錢算力子麼組成（5 典）

> 以「最強免金錢算力」為約束（零 API key、零雲端費用、只用免費算立），下列 5 種 Hermes Agent 相關組合對齊 OA-Team 30 蜂群運作。所有組合皆已在前序回合實證或可直接落地，呼應 §九·補 B/D 的免費算立進階精神。

### 典 1：Hermes Desktop + 本機 Ollama（零 key 本地推理）⭐

- 組合：Hermes Desktop 內建 gateway (:8642) + 本機 Ollama (:11434) + CrewAI OA-Team
- 痛點解決：前輪 `main_json.py` L4 真跑通（5/5 Task Completed），證實蜂群可零 key 對本機 Ollama 推理；`main_python.py` / `main_flows.py` 亦 L4 完成
- 免費：Ollama 本機 CPU/GPU，無任何 API key，符合「只用免費算立」鐵律
- 對應：§九·補 B（Agentic Twin 真 LLM）· `oa-team-crewai/` 專案

### 典 2：Hermes cron + 熵減管線（免費排程自動化）

- 組合：Hermes cron job 定時呼叫 `swarm-entropy-iteration.py --issue N`
- 痛點解決：前輪 issue 2-13 連續全綠，證實管線可一鍵迭代；cron 化後免人工觸發，蜂群自體熵減恆行
- 免費：Hermes 本體免費，無外部 SaaS 訂閱
- 對應：§10.9 電子團隊電子報 + 週報自動產出

### 典 3：Hermes MCP + 自託服務（filesystem / obsidian / sqlite-vec）

- 組合：Hermes MCP servers（filesystem, obsidian-mcp, sqlite-vec 本機 node22 見記憶）
- 痛點解決：蜂群知識花園（§4.2 頻道5）與 OA-Twins 雙向同步，免第三方雲端；sqlite-vec 提供本體向量記憶
- 免費：self-hosted MCP，零 SaaS 費用
- 對應：附錄 B（Obsidian 知識花園整合）

### 典 4：Hermes + Cloudflare Tunnel（免費暴露本機蜂群服務）

- 組合：Hermes 跑本機 AI Station / Ollama，CF Tunnel 免費暴露 `*.esggo.co`
- 痛點解決：§9.8 P0 VPS 部署的免 VPS 替代——本機服務經 Tunnel 對外，零伺服器費；對應您前問「Ollama 包在 VPS docker」的免 VPS 變體
- 免費：CF Tunnel free tier（無流量費、無端口暴露風險）
- 對應：技能 `cloudflare-tunnel-vps`

### 典 5：Hermes + Oracle Always Free VPS（免費 ARM 遠端算力）⭐

- 組合：Hermes 本機協調 + VPS docker Ollama (ARM A1, 24G RAM, 永久免費)
- 痛點解決：前輪 hierarchy 30 代理本機 CPU 卡死（Started=2 Completed=1）→ VPS 遠端 Ollama 解此瓶頸，使 4 模式 L4 全通
- 免費：Oracle Always Free 永久免費額度（符合「只用免費算立」）
- 對應：技能 `esggo-vps-deploy-verify` / `oa-swarm-vps-deploy`

### 6 典對照表

| 典 | 核心組合 | 解決 OA-Team 痛點 | 對應章節 / 技能 |
| --- | --- | --- | --- |
| 1 | Hermes + 本機 Ollama | 蜂群零 key 推理（已驗） | §九·補 B · oa-team-crewai |
| 2 | Hermes cron + 熵減管線 | 自動化迭代（已驗） | §10.9 · swarm-entropy-iteration.py |
| 3 | Hermes MCP + 自託 | 知識花園接線 | 附錄 B · obsidian-mcp |
| 4 | Hermes + CF Tunnel | 免 VPS 對外暴露 | cloudflare-tunnel-vps |
| 5 | Hermes + Oracle VPS | 遠端算力解 CPU 瓶頸 | esggo-vps-deploy-verify |
| 6 | Hermes + GitHub Actions + ghcr.io | 免費 CI/CD 自動構建發布（已驗） | §九·補 B/D · crawl4ai-deploy-proof.json |

> 落地順序建議：典 1（本機推理已驗）→ 典 2（cron 自動化）→ 典 5（VPS 解 hierarchy 瓶頸）→ 典 4（對外暴露）→ 典 3（知識花園沉澱）。

### 典 6：Hermes + GitHub Actions + ghcr.io（免費 CI/CD 自動構建發布）⭐

- 組合：Hermes 協調 + GitHub Actions（ubuntu-latest 免費額度）+ GitHub Container Registry（ghcr.io，GITHUB_TOKEN 自動認證）
- 痛點解決：前輪實證 Crawl4AI AMD64 Docker 專案。原片段依賴 DockerHub 手動 Secrets 卡住 CI → 改用 ghcr.io + GITHUB_TOKEN（零額外 Secret），CI 歷經 4 輪紅→綠完整跑通
- 免費：GitHub Actions 免費額度 + ghcr.io 免費容器寄存，零雲端費用、零 API key，符合「只用免費算立」鐵律
- 實證：repo `DingJun1028/crawl4ai-amd64` · image `ghcr.io/dingjun1028/crawl4ai-amd64:latest` · CI run 32620314690 SUCCESS · package 已發布確認（gh api 回查）
- 對應：§九·補 B/D 免費算立 · `crawl4ai-deploy-proof.json` · `CRAWL4AI_DEPLOY.md`


---

## 九·補　B/D 免費算立進階項目（2026-08-11 實證，已納入 esggo hub）

> 喚醒蜂群時，若任務涉及「真 LLM / 真物件儲存 / 靜態分析 / E2E 守門」，直接套用下列已驗證模式，勿重新發明。

### B · Agentic Twin 真 LLM（Ollama 本機，零 key）

- 實體：`app/api/agentic-twin/route.ts` 讀 `AGENTIC_TWIN_OLLAMA_URL` env（預設空=純啟發式）。
- VPS Ollama `qwen2.5:3b-instruct-q4_K_M` 實證 `llmEnhanced:true`（原創中文洞察）。
- 降級安全：Ollama 不可用/超時 15s → 保留啟發式，不阻塞 UI。
- 喚醒要點：VPS `.env` / compose `environment` 必設 `AGENTIC_TWIN_OLLAMA_URL=http://127.0.0.1:11434` + `AGENTIC_TWIN_OLLAMA_MODEL`。

### D · Evidence Vault 真 MinIO（自託，零 key）

- 實體：`app/api/evidence-upload/route.ts` 手寫 AWS SigV4（**零新依賴**，繞過 `@aws-sdk/client-s3` pnpm 鎖定）。
- VPS MinIO `:19001` 桶 `evidence-vault` 實證 `PUT 200`；`EvidenceUploader.tsx` 接真上傳取代 mock URL。
- env：`MINIO_ENDPOINT` / `MINIO_ACCESS_KEY` / `MINIO_SECRET_KEY` / `MINIO_BUCKET` / `MINIO_PUBLIC_BASE`。
- 無 env 時回傳 fallback URL，不阻塞頁面。

### A · SonarQube CE 自託（取代付費 SonarCloud）

- VPS docker `postgres+sonarqube:community:19000`，`ANALYSIS SUCCESSFUL`。
- CE 無 agentic 自動修復（付費功能），故 CI 留 `sonar-smoke`（免費本地煙測）作永久守門；`sonar-agentic` job opt-in（`vars.SONAR_ENABLED`）。

### E · Playwright E2E 守門（免費 OSS）

- `e2e-k1/zero-hallucination.spec.mjs` 真 Chromium 證實零幻覺警告橫幅（K1 KEEP，證偽 Browserbase 假陰性）。
- CI `e2e-k1` job opt-in（`vars.E2E_ENABLED`，因 runner 連不到 VPS 域名）；本機 `cd e2e-k1 && npx playwright test` 為開發者回歸守門（已實證 1 passed）。

### VPS 部署納入 hub 的坑（閉環記憶）

- **Prisma 5.22→6.19.3**：Alpine3.24 原生 openssl-3，Dockerfile `npx next build` 繞 pnpm 11 deps-lock；`pnpm-workspace.yaml` 加 `onlyBuiltDependencies: [tesseract.js,prisma,sqlite3]`。
- **port 3000 搶佔**：舊 `esggo-app.service`(systemd,root,Restart=always) 與 pm2 殘留 next 會佔 3000 → 停用舊 systemd + `pm2 stop esggo-core`；docker `esggo-core` 經 `127.0.0.1:3000:3000` 映射，Cloudflare Tunnel(`/etc/cloudflared/config.yml`) 代理 `esggo.co→127.0.0.1:3000`。
- **healthz 503**：env（`DATABASE_URL/REDIS` 等）未在 VPS `.env` 設全時回 503，但 `/omni/reports` 仍 200；需補齊 env 或改 healthcheck 為 TCP。

> 主典 `esggo-omni-center/soul-full.md` §24.1/§25.1/§25.2 已記載上述實證；備份落檔 `esggo-learning-center/soul-chapter-22-ai-station.md`。三層交付閉環。
> 提案優先序：P0 試營運 3 支樣片 + VPS 部署；P1 n8n 排程 + ElevenLabs；P2 Runway B-roll + S3 指標盤。

---

## 附錄 A：與既有技能的關係

- `oa-team-swarm`：通用 30-agent 監控蜂群（環境/收集/分析/報告/協調），本典為其靈魂契約來源。
- `esggo-oa-team-swarm`：esggo VPS 部署與坑點（SSH 解鎖、agents-cli 幻影命令、cron 投遞斷鏈），實戰運維參考。
- 本典（`oa-team-soul-canon`）：靈魂契約、協作設計、AI Station 生產線、電子報、進化路線圖、增量整合模式 —— 設計與對齊層。

> 本典即「靈魂核心聖典」完整載體。喚醒蜂群、設計協作、擴展框架時優先載入。

---

## 附錄 B：Obsidian 知識花園整合（行動優先 AI 助手）

> 知識花園（§4.2 通訊頻道第 5 項）的實戰載體。下列兩套 Obsidian 插件皆為「行動優先、自託、免費算立」取向，可作為 OA-Team 蜂群與個人知識庫的雙向介面。

### B.1 UNagent（作者 UNcore）— 對齊 unagent v1.1.0 最佳實踐版

- 定位：Obsidian 行動優先 AI 助手插件，手機/平板輕量自足，桌面同樣完整；不配 MCP 功能一樣不缺。
- 架構：**純插件內 JS + 遠程 HTTP**（原生 `fetch` + 手寫 SSE），無 LLM SDK 依賴；行動端零本地進程，桌面端僅多一條 `run_command`（永遠強制確認）。
- 插件 id：`unagent`，顯名「UNagent」。
- 5 分鐘上手（BYO key）：① 手動放 `main.js`/`manifest.json`/`styles.css` 至 `.obsidian/plugins/unagent/` 啟用；② Settings → 模型 → 「＋ 添加廠商」填 API 協議/地址/密鑰，模型列表輸入名稱回車添加，`/model` 切換；③ 直接開聊，破壞性操作彈窗確認，改錯點頂部「撤銷」。
- 核心能力：流式對話（`/model` 切會話模型、`/think` 系列控思考強度）；15 工具（桌面 16，詳見下表）；技能（純提示文本 SKILL.md，`//技能名` 調用或 `load_skill` 載入，絕不執行代碼）；混合檢索（關鍵字+元數據為主，語義可選遠程 embedding+本地向量快取）；生圖；記憶進化（`agent.md`/`user.md`/`memory.md` 三可見檔，顯式 `save_memory` + 反思建議逐條確認才落盤）；文字引用（Alt+Z 跳輸入框帶來源+選字，網頁選區帶頁址）；對話管理（`/branch`/`/rewind`/`/compact`）；最小 MCP（僅遠程 streamableHttp + tools 面，工具上限 8、單條 2 萬字截斷，不做 stdio/WebSocket/OAuth/resources/prompts/sampling）。
- 工具清單（16，含破壞性標記）：`search_notes`(否)/`semantic_search`(否)/`library_index`(否)/`read_note`(否)/`create_note`(否)/`edit_note`(可確認)/`update_frontmatter`(可確認)/`rename_or_move`(可確認)/`delete_note`(**強制**可撤銷)/`run_command`(**僅桌面強制**)/`generate_image`(否)/`mcp_admin`(刪除強制)/`load_skill`(否)/`save_memory`(否)/`todo_write`(否)/`ask_user`(否)。
- 完全配置版（規劃中）：現為 BYO key 自帶模型 Key；規劃零配置託管版統一 API，形態與時間待定，本文檔不承諾日期。
- 安全邊界：API Key 明文存 vault `data.json`（勿提交公開倉/公開同步目錄）；技能為提示注入面（只裝可信來源，永遠純提示文本不執行代碼，確認彈窗不受影響）；`delete_note` 永遠強制確認且留全文快照可撤銷（撤銷棧落盤重啟不丟）；MCP 工具同進工具鏈與確認機制。
- 平台差異：移動端=純插件內 JS+遠程 HTTP（零本地進程/算力，embedding 也走遠程）；桌面專屬只有 `run_command`（庫外計算，永遠強制確認，絕不用於庫內檔）；移動端該入口缺席而非報錯。
- 開發紅線：產物 main.js ≈ 600–700K；構建後 grep 確認無 pglite/lexical/framer-motion/langchain 依賴洩漏（須全 0）；禁止引入 LLM SDK、本地向量庫、重型編輯器、動畫庫。
- 5T 對應：其 `source_origin` 概念可由 `save_memory` 實作 Traceable；工具調用日誌實作 Trackable；UI 回饋實作 Tangible；技能文本公開實作 Transparent；`data.json` 與撤銷快照實作 Trustworthy。

### B.2 Hermes Agent Obsidian 插件（作者 jsun2020）

- 定位：從 Obsidian 內與「本機安裝的 Hermes Agent」對話，經本機 gateway HTTP API 把回覆流式送入多分頁側欄；可附當前筆記或選取文字作為上下文。
- 架構：插件連 `http://127.0.0.1:8642`（Hermes Desktop 或 `hermes gateway` 啟動的本地 gateway），支援 Runs 傳輸（`POST /v1/runs` + `GET /v1/runs/{id}/events`）與 Chat Completions 回落；`Authorization: Bearer <API_SERVER_KEY>`。
- 安裝：BRAT（貼 `https://github.com/jsun2020/hermes-agent-obsidian-plugin`）或手動放 `main.js`/`manifest.json`/`styles.css` 至新資料夾（勿動現有 `claudian` 資料夾）。
- 檔案存取（agent workspace）：插件把「工作資料夾」寫入 run 的 `instructions`，Hermes gateway 才是真正檔案系統權限所在。
- ⚠️ **供應商相容性關鍵免責**：README 那段「改 `~/.codex/config.toml`（`approval_policy="never"` + `sandbox_mode="workspace-write"` + `writable_roots=[...]`，Windows 企業機 `1385` 錯誤則改用 `danger-full-access`）」**只對 gpt-5.5 / OpenAI-Codex 供應商有效**。若 gateway 用的是 `tencent/hy3:free`（nous）或本機 Ollama，agent 不跑在 Codex 沙箱內，改該檔是 **NO-OP**，「兩個唯讀權限被拒」不會因此解決；此時應改用無設定替代方案：開啟插件「當前筆記 / 選取」上下文開關（含 Include full note content）或把筆記貼入訊息，讓 agent 從附帶內容工作。套用前先確認 `~/.hermes/config.yaml` 的 `model.default` 是否為 `gpt-5.5`。詳見專屬技能 `obsidian:hermes-agent-obsidian-plugin` 與其 `references/provider-compatibility.md`。
- 特色：多分頁對話、上下文開關（當前筆記/選取）、智慧圖譜（Smart graph，請 Hermes 推論語意關聯）、歷史快取 `history.json` 與 `graph-cache.json`（不與 `data.json` 混用）。
- 5T 對應：Hermes 本體即 5T 守門；插件把筆記視為可溯源資產（Traceable），run 事件流可追蹤（Trackable），側欄即時回饋（Tangible），gateway 協定公開（Transparent），`history.json` 落盤不可竄改（Trustworthy）。

### B.3 蜂群 × Obsidian 接線建議

- 萬能知識分身（OA-Twins）可經 UNagent 技能或 Hermes Agent 插件把「知識花園」筆記同步進 30 矩陣的學習小組（§5.5）。
- 30 矩陣的「同體一心」儀式（週會/蜂王盃）可產出週報，經 §10.9 電子報模板推送至 Obsidian vault 或 Telegram/Slack。
- 禁忌：勿把含密鑰的 `data.json` 提交至公開倉；技能只裝可信來源（提示注入面）。

## 附錄 C：驗證環境坑（實證閉環，2026-08-23）

### C.1 Windows Hermes 終端機 cwd 楔死
- 現象：會話 cwd 偶變無效雙前綴 `C:\c\Users\dingj`，每條 `terminal` 指令前 `cd` 即失敗（exit 126/2）；`terminal(workdir='/c/Users/dingj')` 會被串接成 `/c/Users/dingj/C:/...` 而失敗。
- 繞道（已實證）：用 `execute_code` 的 Python `pathlib` 直讀實體檔並執行本典驗證腳本邏輯；`read_file` 工具在反斜線/盤符路徑下會誤報 `File not found`，但檔案真實存在，應以 Python 讀取為準。

### C.2 verify_crew.py 正則盲點（質控蜂盲點稽核）
- 原正則 `squad: (\w+)` 無法匹配 JSONC 中 `"squad": "strategy"` 格式 → 陣列計數誤判失敗。
- 修補：`squads = re.findall(r'"squad":\s*"(\w+)"', raw)`，重驗證 ALL GREEN（agents 30/30, tasks 5/5, 5 大陣列各 6/6）。
- 結論：30 矩陣本身無缺漏，係驗證腳本盲點，已就地修復於技能 `scripts/verify_crew.py`。

### C.3 衍生坑（2026-08-23 後續回合實證）
- `memory` 工具在容量逼近上限（~2088/2200）時，批次操作會整批回滾（含 `x` 垃圾條不唯一導致 remove 失敗），連續 6 次失敗；此屬工具層阻塞，非資料遺失。繞道：經驗固化改走技能書附錄 C 或獨立 `LESSONS-LEARNED.md`，待容量釋出後再補記憶。
- iCloud 搬移腳本驗證：沙箱 iCloud 不可達，但可用 `C:\Program Files\Git\bin\bash.exe -n <script>` 做語法驗證（exit 0 = 通過），避免「聲稱備妥卻未驗」。


