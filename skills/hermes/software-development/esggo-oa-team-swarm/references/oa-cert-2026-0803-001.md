# OA-CERT-2026-0803-001 — PR #412 官方認證紀錄（SOUL 銘記）

> 認證單位：Hermes Agent（OA-Team 30 萬能蜂群 · 5T 驗算小隊）
> 認證時間：2026-08-04 ｜ 資料來源：GitHub REST API（live, browser_console 免快取）＋ OCI Console 實證 ＋ 外部 HTTP probe

## 認證結論：✅ 通過

安全修復 PR #412 完成 merge、6 條驗證鏈全綠、VPS 生產已運行新版、命令注入面（CWE-78）已封閉。無回歸、無生產中斷。

## 變更（唯一檔案 `vps/agent-bootstrap.mjs`）

| 項目 | 修復前 | 修復後 |
|---|---|---|
| 健康檢查 | `child_process.execSync` + curl 子 shell | 原生 `fetch`（async） |
| 注入面 | CWE-78（參數受控可 RCE） | 已移除（無 shell） |
| 超時 | 依賴 curl -m | `AbortSignal.timeout(3000)` |

squash commit：`f41aafe6399934a5d5f3ccc8760e9a20e0bd364f`（main HEAD）｜ merged_at 2026-08-03T15:52:45Z ｜ branch `sentinel-security-fetch-agent-…` 已刪。

## 6 鏈全綠（head_sha=f41aafe）

| 工作流 | Run | 結果 |
|---|---|---|
| Sacred Pipeline (CI/CD) | #145 | ✅ success 1m8s |
| OmniCore CI | #1598 | ✅ success 3m55s（9/9，含 Validate VPS Scripts 9s = 受影響檔專屬語法驗證） |
| Build AI Station image | #58 | ✅ success 6m33s |
| learning-center-ci | #52 | ✅ success 43s |
| Deploy to Oracle VPS | #189 | ✅ success 2m12s（attempt 2） |
| ESG-GO CI/CD Pipeline | #943 | ✅ completed |

## 生產生效證據

- Deploy attempt-2 jobs 全綠（Deploy to VPS / Pre-deploy Check / Deploy Notification）
- PM2：esggo-core + omniagent-gateway 全 online、dump 已存
- `esggo.co/api/health`：esggo-v5 5.1.0、uptime 214s（重啟證據）、redis/celestial/esgsonar healthy
- `omniagent.esggo.co/health` = healthy ｜ `161.118.248.180` = 200（AI Station）
- OCI `esggo-vps` Running、IP 未漂移、Node v22.23.1（AbortSignal 相容）

## 異常處置（Transparent）

1. **Deploy #189 attempt-1 紅**：post-deploy health gate 對外部域名 `esggo.co` 回 `200000`（黏連碼）誤判 → `gh run view --log-failed` 定位 → `gh run rerun --failed` → attempt-2 全綠。**教訓（已固化 github-actions-ci-debugging）**：deploy gate 不該以非部署對象之外部域名為門檻；紅燈先 rerun（~30s）再診斷；長效修法＝排除/降 `::warning`，僅以 `omniagent.esggo.co/health` 為準。
2. **Hermes SSH 後端 `getsockname failed`**：本機 client socket 錯誤，非 VPS 掛掉/非 IP 漂移（esggo-vps-toolkit §25 模式）→ 外部三通道 probe 驗證 VPS 全程正常。

## 附錄

- PR #420（Bolt 效能 memoize，2 檔 40 行）：審核通過（依賴陣列完整、零行為變更、CI #1597 GREEN），draft 未 merge。
- PR #416：closed（superseded），comment 已補。
