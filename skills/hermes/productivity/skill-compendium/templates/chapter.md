# NN · <CHAPTER TITLE>

> 來源：Hermes Agent `<skill-name>` 技能（vX.Y.Z）。
> 整理目標：<one line on what this chapter lets you do>.
> 慣例：本機用 `gh` 優先；若 `gh` 不可用，則用 `git` + `curl` 走 GitHub REST API。

## 0. 前置

```bash
# <any setup / auth / variables needed>
```

## 1. <Subtopic A>

```bash
# gh first
gh ...
# curl fallback
curl -s -X ... -H "Authorization: token $GITHUB_TOKEN" https://api.github.com/...
```

## 2. <Subtopic B>

```bash
# ...
```

## N. 實踐收納清單（本技書專用）

整理時依序檢查：

1. [ ] <check item>
2. [ ] <check item>

## 速查表

| 動作 | gh | git + curl |
|------|-----|-----------|
| ... | `gh ...` | `curl ...` |

## 相關技能

- `<related-skill-a>`：<why>
- `<related-skill-b>`：<why>
