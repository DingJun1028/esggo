/** Firebase config stub — replace with real values from Firebase Console */
export const firebaseConfig = {
  baseUrl: process.env.NEXT_PUBLIC_FIREBASE_API_URL || 'https://firebase-stub.example.com',
  accessToken: process.env.FIREBASE_ACCESS_TOKEN || '',
  projectId: process.env.NEXT_PUBLIC_FIREBASE_PROJECT_ID || '',
};
