services:
  - type: web
    name: esggo-web
    env: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: esggo-db
          property: connectionString

databases:
  - name: esggo-db
    plan: starter
    databaseName: esggo
    user: esggouser
