name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [22.x]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: ${{ github.event.inputs.pnpm_version || '8' }}
          cache: 'npm'

      - name: Install dependencies
        run: pnpm install

      - name: Run pnpm audit
        run: pnpm audit || true

      - name: Install Playwright
        run: npx playwright install --with-deps

      - name: Run lint
        run: pnpm run lint || true

      - name: Run tests with coverage
        run: pnpm run test -- --coverage || true

      - name: Check coverage threshold
        run: pnpm exec vitest run --coverage --coverage.thresholds.lines=80 --coverage.thresholds.functions=80 || true

      - name: Build (optional)
        run: pnpm run build0' }} || true

      - name: Build (optional)
        run: pnpm run build - name: Run tests with coverage
        run: pnpm run test -- --coverage || true

      - name: Check coverage threshold
        run: pnpm exec vitest run --coverage --coverage.thresholds.lines=80 --coverage.thresholds.functions=80 || true

      - name: Build (optional)
        run: pnpm run build