import { expect, test } from 'vitest';
import SkillBookUI from '@/components/omni/SkillBookUI';

test('SkillBookUI default export is a function', () => {
  expect(typeof SkillBookUI).toBe('function');
});
