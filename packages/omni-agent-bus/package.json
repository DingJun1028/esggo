{
  "name": "@esggo/omni-agent-bus",
  "version": "0.1.0",
  "description": "OmniAgentBus — OA 萬能代理總線 (事件/訊息骨幹), 串接 oa-framework 7 子框架 + omni-agent 5T Gate + TencentDB Team Memory",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "tsx test/smoke.ts && tsx test/deploy-gate.smoke.ts && tsx test/oa-bridge.smoke.ts"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "tsx": "^4.19.0"
  },
  "dependencies": {
    "@esggo/oa-framework": "workspace:*"
  }
}
